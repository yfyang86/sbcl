;;;; array VOPs for the WebAssembly target.
;;;;
;;;; Sprint 2 placeholder: the VOPs are written in the next sprint (see
;;;; doc/wasm-port/04-sprints.md). The file exists so that the backend
;;;; builds.

;;;; This software is part of the SBCL system. See the README file for
;;;; more information.

(in-package "SB-VM")

;;;; Skeletons: operand shapes taken from the riscv backend, generators to
;;;; be written in Sprint 3. Generated by Sprints/Sprint2/gen-skeletons.lisp.





















(define-vop (make-array-header)
  (:args (type :scs (any-reg)) (rank :scs (any-reg)))
  (:results (result :scs (descriptor-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:or positive-fixnum fixnum))
  (:translate make-array-header)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'make-array-header type rank result)))

(define-vop (%array-dimension)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (any-reg)))
  (:arg-types * (:or positive-fixnum fixnum))
  (:result-types (:or positive-fixnum))
  (:translate %array-dimension)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented '%array-dimension object index value)))

(define-vop (%array-dimension-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (any-reg)))
  (:arg-types * (:constant (load/store-index 4 7 7)))
  (:result-types (:or positive-fixnum))
  (:info index)
  (:translate %array-dimension)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented '%array-dimension-c object value index)))

(define-vop (%set-array-dimension)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)) (value :scs (any-reg)))
  (:arg-types * (:or positive-fixnum fixnum) (:or positive-fixnum))
  (:translate %set-array-dimension)
  (:policy :fast-safe)
  (:generator 3 (vop-not-yet-implemented '%set-array-dimension object index value)))

(define-vop (%set-array-dimension-c)
  (:args (object :scs (descriptor-reg)) (value :scs (any-reg)))
  (:arg-types * (:constant (load/store-index 4 7 7)) (:or positive-fixnum))
  (:info index)
  (:translate %set-array-dimension)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented '%set-array-dimension-c object value index)))

(define-vop (array-rank)
  (:args (x :scs (descriptor-reg)))
  (:results (res :scs (unsigned-reg)))
  (:result-types (:or positive-fixnum))
  (:translate array-rank)
  (:policy :fast-safe)
  (:generator 6 (vop-not-yet-implemented 'array-rank x res)))

(define-vop (check-bound)
  (:args (array :scs (descriptor-reg)) (bound :scs (any-reg descriptor-reg))
   (index :scs (any-reg descriptor-reg)))
  (:translate %check-bound)
  (:policy :fast-safe)
  (:save-p :compute-only)
  (:variant-vars %test-fixnum)
  (:variant t)
  (:vop-var vop)
  (:generator 6 (vop-not-yet-implemented 'check-bound array bound index %test-fixnum)))

(define-vop (check-bound/fast)
  (:args (array :scs (descriptor-reg)) (bound :scs (any-reg descriptor-reg))
   (index :scs (any-reg descriptor-reg)))
  (:translate %check-bound)
  (:save-p :compute-only)
  (:variant-vars %test-fixnum)
  (:variant nil)
  (:vop-var vop)
  (:generator 4 (vop-not-yet-implemented 'check-bound/fast array bound index %test-fixnum)))

(define-vop (check-bound/fixnum)
  (:args (array :scs (descriptor-reg)) (bound :scs (any-reg descriptor-reg))
   (index :scs (any-reg)))
  (:arg-types * * (:or positive-fixnum fixnum))
  (:translate %check-bound)
  (:policy :fast-safe)
  (:save-p :compute-only)
  (:variant-vars %test-fixnum)
  (:variant nil)
  (:vop-var vop)
  (:generator 4 (vop-not-yet-implemented 'check-bound/fixnum array bound index %test-fixnum)))

(define-vop (check-bound/untagged)
  (:args (array :scs (descriptor-reg)) (bound :scs (unsigned-reg signed-reg))
   (index :scs (unsigned-reg signed-reg)))
  (:arg-types *
   (:or unsigned-byte-32 signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate %check-bound)
  (:policy :fast-safe)
  (:save-p :compute-only)
  (:variant-vars %test-fixnum)
  (:variant nil)
  (:vop-var vop)
  (:generator 5 (vop-not-yet-implemented 'check-bound/untagged array bound index %test-fixnum)))

(define-vop (data-vector-ref/simple-vector)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (descriptor-reg any-reg)))
  (:arg-types (:or simple-vector) (:or positive-fixnum fixnum))
  (:result-types *)
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-ref/simple-vector object index value)))

(define-vop (data-vector-ref/simple-vector-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (descriptor-reg any-reg)))
  (:arg-types (:or simple-vector) (:constant (load/store-index 4 7 2)))
  (:result-types *)
  (:info index)
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'data-vector-ref/simple-vector-c object value index)))

(define-vop (data-vector-set/simple-vector)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (value :scs (descriptor-reg any-reg)))
  (:arg-types (:or simple-vector) (:or positive-fixnum fixnum) *)
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 3 (vop-not-yet-implemented 'data-vector-set/simple-vector object index value)))

(define-vop (data-vector-set/simple-vector-c)
  (:args (object :scs (descriptor-reg)) (value :scs (descriptor-reg any-reg)))
  (:arg-types (:or simple-vector) (:constant (load/store-index 4 7 2)) *)
  (:info index)
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented 'data-vector-set/simple-vector-c object value index)))

(define-vop (data-vector-ref/simple-base-string)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (character-reg)))
  (:arg-types (:or simple-base-string) (:or positive-fixnum))
  (:result-types (:or character))
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-ref/simple-base-string object index value)))

(define-vop (data-vector-ref/simple-base-string-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (character-reg)))
  (:arg-types (:or simple-base-string) (:constant (load/store-index 1 7 2)))
  (:result-types (:or character))
  (:info index)
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'data-vector-ref/simple-base-string-c object value index)))

(define-vop (data-vector-set/simple-base-string)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (value :scs (character-reg)))
  (:arg-types (:or simple-base-string) (:or positive-fixnum) (:or character))
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-set/simple-base-string object index value)))

(define-vop (data-vector-set/simple-base-string-c)
  (:args (object :scs (descriptor-reg)) (value :scs (character-reg)))
  (:arg-types (:or simple-base-string) (:constant (load/store-index 1 7 2))
   (:or character))
  (:info index)
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'data-vector-set/simple-base-string-c object value index)))

(define-vop (data-vector-ref/simple-character-string)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (character-reg)))
  (:arg-types (:or simple-character-string) (:or positive-fixnum fixnum))
  (:result-types (:or character))
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-ref/simple-character-string object index value)))

(define-vop (data-vector-ref/simple-character-string-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (character-reg)))
  (:arg-types (:or simple-character-string) (:constant (load/store-index 4 7 2)))
  (:result-types (:or character))
  (:info index)
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'data-vector-ref/simple-character-string-c object value index)))

(define-vop (data-vector-set/simple-character-string)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (value :scs (character-reg)))
  (:arg-types (:or simple-character-string) (:or positive-fixnum fixnum) (:or character))
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 3 (vop-not-yet-implemented 'data-vector-set/simple-character-string object index value)))

(define-vop (data-vector-set/simple-character-string-c)
  (:args (object :scs (descriptor-reg)) (value :scs (character-reg)))
  (:arg-types (:or simple-character-string) (:constant (load/store-index 4 7 2))
   (:or character))
  (:info index)
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented 'data-vector-set/simple-character-string-c object value index)))

(define-vop (data-vector-ref/simple-array-unsigned-byte-7)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (unsigned-reg signed-reg)))
  (:arg-types (:or simple-array-unsigned-byte-7) (:or positive-fixnum))
  (:result-types (:or positive-fixnum))
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-ref/simple-array-unsigned-byte-7 object index value)))

(define-vop (data-vector-ref/simple-array-unsigned-byte-7-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (unsigned-reg signed-reg)))
  (:arg-types (:or simple-array-unsigned-byte-7) (:constant (load/store-index 1 7 2)))
  (:result-types (:or positive-fixnum))
  (:info index)
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:ignore object value index)
  (:generator 4
   (vop-not-yet-implemented 'data-vector-ref/simple-array-unsigned-byte-7-c)))

(define-vop (data-vector-set/simple-array-unsigned-byte-7)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (value :scs (unsigned-reg signed-reg)))
  (:arg-types (:or simple-array-unsigned-byte-7) (:or positive-fixnum)
   (:or positive-fixnum))
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-set/simple-array-unsigned-byte-7 object index value)))

(define-vop (data-vector-set/simple-array-unsigned-byte-7-c)
  (:args (object :scs (descriptor-reg)) (value :scs (unsigned-reg signed-reg)))
  (:arg-types (:or simple-array-unsigned-byte-7) (:constant (load/store-index 1 7 2))
   (:or positive-fixnum))
  (:info index)
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:ignore object value index)
  (:generator 4
   (vop-not-yet-implemented 'data-vector-set/simple-array-unsigned-byte-7-c)))

(define-vop (data-vector-ref/simple-array-signed-byte-8)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (signed-reg)))
  (:arg-types (:or simple-array-signed-byte-8) (:or positive-fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-ref/simple-array-signed-byte-8 object index value)))

(define-vop (data-vector-ref/simple-array-signed-byte-8-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (signed-reg)))
  (:arg-types (:or simple-array-signed-byte-8) (:constant (load/store-index 1 7 2)))
  (:result-types (:or positive-fixnum fixnum))
  (:info index)
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'data-vector-ref/simple-array-signed-byte-8-c object value index)))

(define-vop (data-vector-set/simple-array-signed-byte-8)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)) (value :scs (signed-reg)))
  (:arg-types (:or simple-array-signed-byte-8) (:or positive-fixnum)
   (:or positive-fixnum fixnum))
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-set/simple-array-signed-byte-8 object index value)))

(define-vop (data-vector-set/simple-array-signed-byte-8-c)
  (:args (object :scs (descriptor-reg)) (value :scs (signed-reg)))
  (:arg-types (:or simple-array-signed-byte-8) (:constant (load/store-index 1 7 2))
   (:or positive-fixnum fixnum))
  (:info index)
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'data-vector-set/simple-array-signed-byte-8-c object value index)))

(define-vop (data-vector-ref/simple-array-unsigned-byte-8)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (unsigned-reg signed-reg)))
  (:arg-types (:or simple-array-unsigned-byte-8) (:or positive-fixnum))
  (:result-types (:or positive-fixnum))
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-ref/simple-array-unsigned-byte-8 object index value)))

(define-vop (data-vector-ref/simple-array-unsigned-byte-8-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (unsigned-reg signed-reg)))
  (:arg-types (:or simple-array-unsigned-byte-8) (:constant (load/store-index 1 7 2)))
  (:result-types (:or positive-fixnum))
  (:info index)
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:ignore object value index)
  (:generator 4
   (vop-not-yet-implemented 'data-vector-ref/simple-array-unsigned-byte-8-c)))

(define-vop (data-vector-set/simple-array-unsigned-byte-8)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (value :scs (unsigned-reg signed-reg)))
  (:arg-types (:or simple-array-unsigned-byte-8) (:or positive-fixnum)
   (:or positive-fixnum))
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-set/simple-array-unsigned-byte-8 object index value)))

(define-vop (data-vector-set/simple-array-unsigned-byte-8-c)
  (:args (object :scs (descriptor-reg)) (value :scs (unsigned-reg signed-reg)))
  (:arg-types (:or simple-array-unsigned-byte-8) (:constant (load/store-index 1 7 2))
   (:or positive-fixnum))
  (:info index)
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:ignore object value index)
  (:generator 4
   (vop-not-yet-implemented 'data-vector-set/simple-array-unsigned-byte-8-c)))

(define-vop (data-vector-ref/simple-array-unsigned-byte-15)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (unsigned-reg signed-reg)))
  (:arg-types (:or simple-array-unsigned-byte-15) (:or positive-fixnum))
  (:result-types (:or positive-fixnum))
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-ref/simple-array-unsigned-byte-15 object index value)))

(define-vop (data-vector-ref/simple-array-unsigned-byte-15-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (unsigned-reg signed-reg)))
  (:arg-types (:or simple-array-unsigned-byte-15) (:constant (load/store-index 2 7 2)))
  (:result-types (:or positive-fixnum))
  (:info index)
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:ignore object value index)
  (:generator 4
   (vop-not-yet-implemented 'data-vector-ref/simple-array-unsigned-byte-15-c)))

(define-vop (data-vector-set/simple-array-unsigned-byte-15)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (value :scs (unsigned-reg signed-reg)))
  (:arg-types (:or simple-array-unsigned-byte-15) (:or positive-fixnum)
   (:or positive-fixnum))
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-set/simple-array-unsigned-byte-15 object index value)))

(define-vop (data-vector-set/simple-array-unsigned-byte-15-c)
  (:args (object :scs (descriptor-reg)) (value :scs (unsigned-reg signed-reg)))
  (:arg-types (:or simple-array-unsigned-byte-15) (:constant (load/store-index 2 7 2))
   (:or positive-fixnum))
  (:info index)
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:ignore object value index)
  (:generator 4
   (vop-not-yet-implemented 'data-vector-set/simple-array-unsigned-byte-15-c)))

(define-vop (data-vector-ref/simple-array-signed-byte-16)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (signed-reg)))
  (:arg-types (:or simple-array-signed-byte-16) (:or positive-fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-ref/simple-array-signed-byte-16 object index value)))

(define-vop (data-vector-ref/simple-array-signed-byte-16-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (signed-reg)))
  (:arg-types (:or simple-array-signed-byte-16) (:constant (load/store-index 2 7 2)))
  (:result-types (:or positive-fixnum fixnum))
  (:info index)
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'data-vector-ref/simple-array-signed-byte-16-c object value index)))

(define-vop (data-vector-set/simple-array-signed-byte-16)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)) (value :scs (signed-reg)))
  (:arg-types (:or simple-array-signed-byte-16) (:or positive-fixnum)
   (:or positive-fixnum fixnum))
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-set/simple-array-signed-byte-16 object index value)))

(define-vop (data-vector-set/simple-array-signed-byte-16-c)
  (:args (object :scs (descriptor-reg)) (value :scs (signed-reg)))
  (:arg-types (:or simple-array-signed-byte-16) (:constant (load/store-index 2 7 2))
   (:or positive-fixnum fixnum))
  (:info index)
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'data-vector-set/simple-array-signed-byte-16-c object value index)))

(define-vop (data-vector-ref/simple-array-unsigned-byte-16)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (unsigned-reg signed-reg)))
  (:arg-types (:or simple-array-unsigned-byte-16) (:or positive-fixnum))
  (:result-types (:or positive-fixnum))
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-ref/simple-array-unsigned-byte-16 object index value)))

(define-vop (data-vector-ref/simple-array-unsigned-byte-16-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (unsigned-reg signed-reg)))
  (:arg-types (:or simple-array-unsigned-byte-16) (:constant (load/store-index 2 7 2)))
  (:result-types (:or positive-fixnum))
  (:info index)
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:ignore object value index)
  (:generator 4
   (vop-not-yet-implemented 'data-vector-ref/simple-array-unsigned-byte-16-c)))

(define-vop (data-vector-set/simple-array-unsigned-byte-16)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (value :scs (unsigned-reg signed-reg)))
  (:arg-types (:or simple-array-unsigned-byte-16) (:or positive-fixnum)
   (:or positive-fixnum))
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-set/simple-array-unsigned-byte-16 object index value)))

(define-vop (data-vector-set/simple-array-unsigned-byte-16-c)
  (:args (object :scs (descriptor-reg)) (value :scs (unsigned-reg signed-reg)))
  (:arg-types (:or simple-array-unsigned-byte-16) (:constant (load/store-index 2 7 2))
   (:or positive-fixnum))
  (:info index)
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:ignore object value index)
  (:generator 4
   (vop-not-yet-implemented 'data-vector-set/simple-array-unsigned-byte-16-c)))

(define-vop (data-vector-ref/simple-array-unsigned-byte-31)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (unsigned-reg)))
  (:arg-types (:or simple-array-unsigned-byte-31) (:or positive-fixnum fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-ref/simple-array-unsigned-byte-31 object index value)))

(define-vop (data-vector-ref/simple-array-unsigned-byte-31-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (unsigned-reg)))
  (:arg-types (:or simple-array-unsigned-byte-31) (:constant (load/store-index 4 7 2)))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:info index)
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:ignore object value index)
  (:generator 4
   (vop-not-yet-implemented 'data-vector-ref/simple-array-unsigned-byte-31-c)))

(define-vop (data-vector-set/simple-array-unsigned-byte-31)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (value :scs (unsigned-reg)))
  (:arg-types (:or simple-array-unsigned-byte-31) (:or positive-fixnum fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 3 (vop-not-yet-implemented 'data-vector-set/simple-array-unsigned-byte-31 object index value)))

(define-vop (data-vector-set/simple-array-unsigned-byte-31-c)
  (:args (object :scs (descriptor-reg)) (value :scs (unsigned-reg)))
  (:arg-types (:or simple-array-unsigned-byte-31) (:constant (load/store-index 4 7 2))
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:info index)
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:ignore object value index)
  (:generator 1
   (vop-not-yet-implemented 'data-vector-set/simple-array-unsigned-byte-31-c)))

(define-vop (data-vector-ref/simple-array-signed-byte-32)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (signed-reg)))
  (:arg-types (:or simple-array-signed-byte-32) (:or positive-fixnum fixnum))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-ref/simple-array-signed-byte-32 object index value)))

(define-vop (data-vector-ref/simple-array-signed-byte-32-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (signed-reg)))
  (:arg-types (:or simple-array-signed-byte-32) (:constant (load/store-index 4 7 2)))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:info index)
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'data-vector-ref/simple-array-signed-byte-32-c object value index)))

(define-vop (data-vector-set/simple-array-signed-byte-32)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)) (value :scs (signed-reg)))
  (:arg-types (:or simple-array-signed-byte-32) (:or positive-fixnum fixnum)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 3 (vop-not-yet-implemented 'data-vector-set/simple-array-signed-byte-32 object index value)))

(define-vop (data-vector-set/simple-array-signed-byte-32-c)
  (:args (object :scs (descriptor-reg)) (value :scs (signed-reg)))
  (:arg-types (:or simple-array-signed-byte-32) (:constant (load/store-index 4 7 2))
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:info index)
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented 'data-vector-set/simple-array-signed-byte-32-c object value index)))

(define-vop (data-vector-ref/simple-array-unsigned-byte-32)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (unsigned-reg)))
  (:arg-types (:or simple-array-unsigned-byte-32) (:or positive-fixnum fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-ref/simple-array-unsigned-byte-32 object index value)))

(define-vop (data-vector-ref/simple-array-unsigned-byte-32-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (unsigned-reg)))
  (:arg-types (:or simple-array-unsigned-byte-32) (:constant (load/store-index 4 7 2)))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:info index)
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:ignore object value index)
  (:generator 4
   (vop-not-yet-implemented 'data-vector-ref/simple-array-unsigned-byte-32-c)))

(define-vop (data-vector-set/simple-array-unsigned-byte-32)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (value :scs (unsigned-reg)))
  (:arg-types (:or simple-array-unsigned-byte-32) (:or positive-fixnum fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 3 (vop-not-yet-implemented 'data-vector-set/simple-array-unsigned-byte-32 object index value)))

(define-vop (data-vector-set/simple-array-unsigned-byte-32-c)
  (:args (object :scs (descriptor-reg)) (value :scs (unsigned-reg)))
  (:arg-types (:or simple-array-unsigned-byte-32) (:constant (load/store-index 4 7 2))
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:info index)
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:ignore object value index)
  (:generator 1
   (vop-not-yet-implemented 'data-vector-set/simple-array-unsigned-byte-32-c)))

(define-vop (data-vector-ref/simple-array-unsigned-fixnum)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (any-reg)))
  (:arg-types (:or simple-array-unsigned-fixnum) (:or positive-fixnum fixnum))
  (:result-types (:or positive-fixnum))
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-ref/simple-array-unsigned-fixnum object index value)))

(define-vop (data-vector-ref/simple-array-unsigned-fixnum-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (any-reg)))
  (:arg-types (:or simple-array-unsigned-fixnum) (:constant (load/store-index 4 7 2)))
  (:result-types (:or positive-fixnum))
  (:info index)
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:ignore object value index)
  (:generator 4
   (vop-not-yet-implemented 'data-vector-ref/simple-array-unsigned-fixnum-c)))

(define-vop (data-vector-set/simple-array-unsigned-fixnum)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)) (value :scs (any-reg)))
  (:arg-types (:or simple-array-unsigned-fixnum) (:or positive-fixnum fixnum)
   (:or positive-fixnum))
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 3 (vop-not-yet-implemented 'data-vector-set/simple-array-unsigned-fixnum object index value)))

(define-vop (data-vector-set/simple-array-unsigned-fixnum-c)
  (:args (object :scs (descriptor-reg)) (value :scs (any-reg)))
  (:arg-types (:or simple-array-unsigned-fixnum) (:constant (load/store-index 4 7 2))
   (:or positive-fixnum))
  (:info index)
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:ignore object value index)
  (:generator 1
   (vop-not-yet-implemented 'data-vector-set/simple-array-unsigned-fixnum-c)))

(define-vop (data-vector-ref/simple-array-fixnum)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (any-reg)))
  (:arg-types (:or simple-array-fixnum) (:or positive-fixnum fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-ref/simple-array-fixnum object index value)))

(define-vop (data-vector-ref/simple-array-fixnum-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (any-reg)))
  (:arg-types (:or simple-array-fixnum) (:constant (load/store-index 4 7 2)))
  (:result-types (:or positive-fixnum fixnum))
  (:info index)
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'data-vector-ref/simple-array-fixnum-c object value index)))

(define-vop (data-vector-set/simple-array-fixnum)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)) (value :scs (any-reg)))
  (:arg-types (:or simple-array-fixnum) (:or positive-fixnum fixnum)
   (:or positive-fixnum fixnum))
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 3 (vop-not-yet-implemented 'data-vector-set/simple-array-fixnum object index value)))

(define-vop (data-vector-set/simple-array-fixnum-c)
  (:args (object :scs (descriptor-reg)) (value :scs (any-reg)))
  (:arg-types (:or simple-array-fixnum) (:constant (load/store-index 4 7 2))
   (:or positive-fixnum fixnum))
  (:info index)
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented 'data-vector-set/simple-array-fixnum-c object value index)))

(define-vop (data-vector-ref/simple-array-single-float)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (single-reg)))
  (:arg-types (:or simple-array-single-float) (:or positive-fixnum fixnum))
  (:result-types (:or single-float))
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:note "inline array access")
  (:generator 5 (vop-not-yet-implemented 'data-vector-ref/simple-array-single-float object index value)))

(define-vop (data-vector-ref/simple-array-single-float-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (single-reg)))
  (:arg-types (:or simple-array-single-float) (:constant (load/store-index 4 7 2)))
  (:result-types (:or single-float))
  (:info index)
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:note "inline array access")
  (:generator 4 (vop-not-yet-implemented 'data-vector-ref/simple-array-single-float-c object value index)))

(define-vop (data-vector-set/simple-array-single-float)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)) (value :scs (single-reg)))
  (:arg-types (:or simple-array-single-float) (:or positive-fixnum fixnum)
   (:or single-float))
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:note "inline array store")
  (:generator 5 (vop-not-yet-implemented 'data-vector-set/simple-array-single-float object index value)))

(define-vop (data-vector-set/simple-array-single-float-c)
  (:args (object :scs (descriptor-reg)) (value :scs (single-reg)))
  (:arg-types (:or simple-array-single-float) (:constant (load/store-index 4 7 2))
   (:or single-float))
  (:info index)
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:note "inline array store")
  (:generator 4 (vop-not-yet-implemented 'data-vector-set/simple-array-single-float-c object value index)))

(define-vop (data-vector-ref/simple-array-double-float)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (double-reg)))
  (:arg-types (:or simple-array-double-float) (:or positive-fixnum fixnum))
  (:result-types (:or double-float))
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:note "inline array access")
  (:generator 5 (vop-not-yet-implemented 'data-vector-ref/simple-array-double-float object index value)))

(define-vop (data-vector-ref/simple-array-double-float-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (double-reg)))
  (:arg-types (:or simple-array-double-float) (:constant (load/store-index 8 7 2)))
  (:result-types (:or double-float))
  (:info index)
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:note "inline array access")
  (:generator 4 (vop-not-yet-implemented 'data-vector-ref/simple-array-double-float-c object value index)))

(define-vop (data-vector-set/simple-array-double-float)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)) (value :scs (double-reg)))
  (:arg-types (:or simple-array-double-float) (:or positive-fixnum fixnum)
   (:or double-float))
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:note "inline array store")
  (:generator 5 (vop-not-yet-implemented 'data-vector-set/simple-array-double-float object index value)))

(define-vop (data-vector-set/simple-array-double-float-c)
  (:args (object :scs (descriptor-reg)) (value :scs (double-reg)))
  (:arg-types (:or simple-array-double-float) (:constant (load/store-index 8 7 2))
   (:or double-float))
  (:info index)
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:note "inline array store")
  (:generator 4 (vop-not-yet-implemented 'data-vector-set/simple-array-double-float-c object value index)))

(define-vop (data-vector-ref/simple-array-complex-single-float)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (complex-single-reg)))
  (:arg-types (:or simple-array-complex-single-float) (:or positive-fixnum fixnum))
  (:result-types (:or complex-single-float))
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:note "inline array access")
  (:ignore object index value)
  (:generator 6
   (vop-not-yet-implemented 'data-vector-ref/simple-array-complex-single-float)))

(define-vop (data-vector-set/simple-array-complex-single-float)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (value :scs (complex-single-reg)))
  (:arg-types (:or simple-array-complex-single-float) (:or positive-fixnum fixnum)
   (:or complex-single-float))
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:note "inline array store")
  (:ignore object index value)
  (:generator 6
   (vop-not-yet-implemented 'data-vector-set/simple-array-complex-single-float)))

(define-vop (data-vector-ref/simple-array-complex-double-float)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (complex-double-reg)))
  (:arg-types (:or simple-array-complex-double-float) (:or positive-fixnum fixnum))
  (:result-types (:or complex-double-float))
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:note "inline array access")
  (:ignore object index value)
  (:generator 6
   (vop-not-yet-implemented 'data-vector-ref/simple-array-complex-double-float)))

(define-vop (data-vector-set/simple-array-complex-double-float)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (value :scs (complex-double-reg)))
  (:arg-types (:or simple-array-complex-double-float) (:or positive-fixnum fixnum)
   (:or complex-double-float))
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:note "inline array store")
  (:ignore object index value)
  (:generator 6
   (vop-not-yet-implemented 'data-vector-set/simple-array-complex-double-float)))

(define-vop (data-vector-ref/simple-bit-vector)
  (:args (object :scs (descriptor-reg)) (index :scs (unsigned-reg)))
  (:results (value :scs (any-reg)))
  (:arg-types (:or simple-bit-vector) (:or positive-fixnum))
  (:result-types (:or positive-fixnum))
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:note "inline array access")
  (:generator 20 (vop-not-yet-implemented 'data-vector-ref/simple-bit-vector object index value)))

(define-vop (data-vector-ref-c/simple-bit-vector)
  (:args (object :scs (descriptor-reg)))
  (:results (result :scs (unsigned-reg)))
  (:arg-types (:or simple-bit-vector) (:constant (integer 0 16383)))
  (:result-types (:or positive-fixnum))
  (:info index)
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:generator 15 (vop-not-yet-implemented 'data-vector-ref-c/simple-bit-vector object result index)))

(define-vop (data-vector-set/simple-bit-vector)
  (:args (object :scs (descriptor-reg)) (index :scs (unsigned-reg))
   (value :scs (unsigned-reg immediate)))
  (:arg-types (:or simple-bit-vector) (:or positive-fixnum) (:or positive-fixnum))
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:note "inline array store")
  (:generator 25 (vop-not-yet-implemented 'data-vector-set/simple-bit-vector object index value)))

(define-vop (data-vector-set-c/simple-bit-vector)
  (:args (object :scs (descriptor-reg)) (value :scs (unsigned-reg immediate)))
  (:arg-types (:or simple-bit-vector) (:constant (integer 0 16383)) (:or positive-fixnum))
  (:info index)
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:generator 20 (vop-not-yet-implemented 'data-vector-set-c/simple-bit-vector object value index)))

(define-vop (data-vector-ref/simple-array-unsigned-byte-2)
  (:args (object :scs (descriptor-reg)) (index :scs (unsigned-reg)))
  (:results (value :scs (any-reg)))
  (:arg-types (:or simple-array-unsigned-byte-2) (:or positive-fixnum))
  (:result-types (:or positive-fixnum))
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:note "inline array access")
  (:generator 20 (vop-not-yet-implemented 'data-vector-ref/simple-array-unsigned-byte-2 object index value)))

(define-vop (data-vector-ref-c/simple-array-unsigned-byte-2)
  (:args (object :scs (descriptor-reg)))
  (:results (result :scs (unsigned-reg)))
  (:arg-types (:or simple-array-unsigned-byte-2) (:constant (integer 0 8191)))
  (:result-types (:or positive-fixnum))
  (:info index)
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:ignore object result index)
  (:generator 15
   (vop-not-yet-implemented 'data-vector-ref-c/simple-array-unsigned-byte-2)))

(define-vop (data-vector-set/simple-array-unsigned-byte-2)
  (:args (object :scs (descriptor-reg)) (index :scs (unsigned-reg))
   (value :scs (unsigned-reg immediate)))
  (:arg-types (:or simple-array-unsigned-byte-2) (:or positive-fixnum)
   (:or positive-fixnum))
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:note "inline array store")
  (:generator 25 (vop-not-yet-implemented 'data-vector-set/simple-array-unsigned-byte-2 object index value)))

(define-vop (data-vector-set-c/simple-array-unsigned-byte-2)
  (:args (object :scs (descriptor-reg)) (value :scs (unsigned-reg immediate)))
  (:arg-types (:or simple-array-unsigned-byte-2) (:constant (integer 0 8191))
   (:or positive-fixnum))
  (:info index)
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:ignore object value index)
  (:generator 20
   (vop-not-yet-implemented 'data-vector-set-c/simple-array-unsigned-byte-2)))

(define-vop (data-vector-ref/simple-array-unsigned-byte-4)
  (:args (object :scs (descriptor-reg)) (index :scs (unsigned-reg)))
  (:results (value :scs (any-reg)))
  (:arg-types (:or simple-array-unsigned-byte-4) (:or positive-fixnum))
  (:result-types (:or positive-fixnum))
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:note "inline array access")
  (:generator 20 (vop-not-yet-implemented 'data-vector-ref/simple-array-unsigned-byte-4 object index value)))

(define-vop (data-vector-ref-c/simple-array-unsigned-byte-4)
  (:args (object :scs (descriptor-reg)))
  (:results (result :scs (unsigned-reg)))
  (:arg-types (:or simple-array-unsigned-byte-4) (:constant (integer 0 4095)))
  (:result-types (:or positive-fixnum))
  (:info index)
  (:translate data-vector-ref)
  (:policy :fast-safe)
  (:ignore object result index)
  (:generator 15
   (vop-not-yet-implemented 'data-vector-ref-c/simple-array-unsigned-byte-4)))

(define-vop (data-vector-set/simple-array-unsigned-byte-4)
  (:args (object :scs (descriptor-reg)) (index :scs (unsigned-reg))
   (value :scs (unsigned-reg immediate)))
  (:arg-types (:or simple-array-unsigned-byte-4) (:or positive-fixnum)
   (:or positive-fixnum))
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:note "inline array store")
  (:generator 25 (vop-not-yet-implemented 'data-vector-set/simple-array-unsigned-byte-4 object index value)))

(define-vop (data-vector-set-c/simple-array-unsigned-byte-4)
  (:args (object :scs (descriptor-reg)) (value :scs (unsigned-reg immediate)))
  (:arg-types (:or simple-array-unsigned-byte-4) (:constant (integer 0 4095))
   (:or positive-fixnum))
  (:info index)
  (:translate data-vector-set)
  (:policy :fast-safe)
  (:ignore object value index)
  (:generator 20
   (vop-not-yet-implemented 'data-vector-set-c/simple-array-unsigned-byte-4)))

(define-vop (vector-raw-bits)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (unsigned-reg)))
  (:arg-types * (:or positive-fixnum fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate %vector-raw-bits)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'vector-raw-bits object index value)))

(define-vop (vector-raw-bits-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (unsigned-reg)))
  (:arg-types * (:constant (load/store-index 4 7 2)))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:info index)
  (:translate %vector-raw-bits)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'vector-raw-bits-c object value index)))

(define-vop (set-vector-raw-bits)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (value :scs (unsigned-reg)))
  (:arg-types * (:or positive-fixnum fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate %set-vector-raw-bits)
  (:policy :fast-safe)
  (:generator 3 (vop-not-yet-implemented 'set-vector-raw-bits object index value)))

(define-vop (set-vector-raw-bits-c)
  (:args (object :scs (descriptor-reg)) (value :scs (unsigned-reg)))
  (:arg-types * (:constant (load/store-index 4 7 2))
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:info index)
  (:translate %set-vector-raw-bits)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented 'set-vector-raw-bits-c object value index)))

(define-vop (data-vector-cas/simple-vector)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (old-value :scs (any-reg descriptor-reg)) (new-value :scs (any-reg descriptor-reg)))
  (:results (result :scs (any-reg descriptor-reg)))
  (:arg-types (:or simple-vector) (:or positive-fixnum) * *)
  (:result-types *)
  (:translate %compare-and-swap-svref)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'data-vector-cas/simple-vector object index old-value new-value result)))

(define-vop (array-atomic-incf/word)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (operand :scs (unsigned-reg)))
  (:results (result :scs (unsigned-reg)))
  (:arg-types * (:or positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate %array-atomic-incf/word)
  (:policy :fast-safe)
  (:generator 3 (vop-not-yet-implemented 'array-atomic-incf/word object index operand result)))
