;;;; arith VOPs for the WebAssembly target.
;;;;
;;;; Sprint 2 placeholder: the VOPs are written in the next sprint (see
;;;; doc/wasm-port/04-sprints.md). The file exists so that the backend
;;;; builds.

;;;; This software is part of the SBCL system. See the README file for
;;;; more information.

(in-package "SB-VM")

;;;; Skeletons: operand shapes taken from the riscv backend, generators to
;;;; be written in Sprint 3. Generated by Sprints/Sprint2/gen-skeletons.lisp.

(define-vop (fast-safe-arith-op)
  (:args)
  (:policy :fast-safe)
  (:generator 0 (vop-not-yet-implemented 'fast-safe-arith-op )))

(define-vop (fixnum-unop)
  (:args (x :scs (any-reg)))
  (:results (res :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:policy :fast-safe)
  (:note "inline fixnum arithmetic")
  (:generator 0 (vop-not-yet-implemented 'fixnum-unop x res)))

(define-vop (signed-unop)
  (:args (x :scs (signed-reg)))
  (:results (res :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:policy :fast-safe)
  (:note "inline (signed-byte 32) arithmetic")
  (:generator 0 (vop-not-yet-implemented 'signed-unop x res)))

(define-vop (fast-negate/fixnum)
  (:args (x :scs (any-reg)))
  (:results (res :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:translate %negate)
  (:policy :fast-safe)
  (:note "inline fixnum arithmetic")
  (:generator 1 (vop-not-yet-implemented 'fast-negate/fixnum x res)))

(define-vop (fast-negate/signed)
  (:args (x :scs (signed-reg)))
  (:results (res :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate %negate)
  (:policy :fast-safe)
  (:note "inline (signed-byte 32) arithmetic")
  (:generator 2 (vop-not-yet-implemented 'fast-negate/signed x res)))

(define-vop (fast-lognot/fixnum)
  (:args (x :scs (any-reg)))
  (:results (res :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:translate lognot)
  (:policy :fast-safe)
  (:note "inline fixnum arithmetic")
  (:generator 1 (vop-not-yet-implemented 'fast-lognot/fixnum x res)))

(define-vop (fast-lognot/signed)
  (:args (x :scs (signed-reg)))
  (:results (res :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate lognot)
  (:policy :fast-safe)
  (:note "inline (signed-byte 32) arithmetic")
  (:generator 2 (vop-not-yet-implemented 'fast-lognot/signed x res)))

(define-vop (fast-fixnum-binop)
  (:args (x :scs (any-reg)) (y :scs (any-reg)))
  (:results (r :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:or positive-fixnum fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:policy :fast-safe)
  (:note "inline fixnum arithmetic")
  (:generator 0 (vop-not-yet-implemented 'fast-fixnum-binop x y r)))

(define-vop (fast-unsigned-binop)
  (:args (x :scs (unsigned-reg)) (y :scs (unsigned-reg)))
  (:results (r :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:policy :fast-safe)
  (:note "inline (unsigned-byte 32) arithmetic")
  (:generator 0 (vop-not-yet-implemented 'fast-unsigned-binop x y r)))

(define-vop (fast-signed-binop)
  (:args (x :scs (signed-reg)) (y :scs (signed-reg)))
  (:results (r :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:policy :fast-safe)
  (:note "inline (signed-byte 32) arithmetic")
  (:generator 0 (vop-not-yet-implemented 'fast-signed-binop x y r)))

(define-vop (fast-fixnum-binop-c)
  (:args (x :scs (any-reg)))
  (:results (r :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:constant short-immediate-fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:info y)
  (:policy :fast-safe)
  (:note "inline arithmetic")
  (:generator 0 (vop-not-yet-implemented 'fast-fixnum-binop-c x r y)))

(define-vop (fast-unsigned-binop-c)
  (:args (x :scs (unsigned-reg)))
  (:results (r :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:constant short-immediate))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:info y)
  (:policy :fast-safe)
  (:note "inline unsigned unboxed arithmetic")
  (:generator 0 (vop-not-yet-implemented 'fast-unsigned-binop-c x r y)))

(define-vop (fast-signed-binop-c)
  (:args (x :scs (signed-reg)))
  (:results (r :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:constant short-immediate))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:info y)
  (:policy :fast-safe)
  (:note "inline signed unboxed arithmetic")
  (:generator 0 (vop-not-yet-implemented 'fast-signed-binop-c x r y)))

(define-vop (fast-+/fixnum=>fixnum)
  (:args (x :scs (any-reg)) (y :scs (any-reg)))
  (:results (r :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:or positive-fixnum fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:translate +)
  (:policy :fast-safe)
  (:note "inline fixnum arithmetic")
  (:generator 2 (vop-not-yet-implemented 'fast-+/fixnum=>fixnum x y r)))

(define-vop (fast-+/signed=>signed)
  (:args (x :scs (signed-reg)) (y :scs (signed-reg)))
  (:results (r :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate +)
  (:policy :fast-safe)
  (:note "inline (signed-byte 32) arithmetic")
  (:generator 6 (vop-not-yet-implemented 'fast-+/signed=>signed x y r)))

(define-vop (fast-+/unsigned=>unsigned)
  (:args (x :scs (unsigned-reg)) (y :scs (unsigned-reg)))
  (:results (r :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate +)
  (:policy :fast-safe)
  (:note "inline (unsigned-byte 32) arithmetic")
  (:generator 6 (vop-not-yet-implemented 'fast-+/unsigned=>unsigned x y r)))

(define-vop (fast-+-c/fixnum=>fixnum)
  (:args (x :scs (any-reg)))
  (:results (r :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:constant short-immediate-fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:info y)
  (:translate +)
  (:policy :fast-safe)
  (:note "inline arithmetic")
  (:generator 1 (vop-not-yet-implemented 'fast-+-c/fixnum=>fixnum x r y)))

(define-vop (fast-+-c/signed=>signed)
  (:args (x :scs (signed-reg)))
  (:results (r :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:constant short-immediate))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:info y)
  (:translate +)
  (:policy :fast-safe)
  (:note "inline signed unboxed arithmetic")
  (:generator 5 (vop-not-yet-implemented 'fast-+-c/signed=>signed x r y)))

(define-vop (fast-+-c/unsigned=>unsigned)
  (:args (x :scs (unsigned-reg)))
  (:results (r :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:constant short-immediate))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:info y)
  (:translate +)
  (:policy :fast-safe)
  (:note "inline unsigned unboxed arithmetic")
  (:generator 5 (vop-not-yet-implemented 'fast-+-c/unsigned=>unsigned x r y)))

(define-vop (fast--/fixnum=>fixnum)
  (:args (x :scs (any-reg)) (y :scs (any-reg)))
  (:results (r :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:or positive-fixnum fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:translate -)
  (:policy :fast-safe)
  (:note "inline fixnum arithmetic")
  (:generator 2 (vop-not-yet-implemented 'fast--/fixnum=>fixnum x y r)))

(define-vop (fast--/signed=>signed)
  (:args (x :scs (signed-reg)) (y :scs (signed-reg)))
  (:results (r :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate -)
  (:policy :fast-safe)
  (:note "inline (signed-byte 32) arithmetic")
  (:generator 6 (vop-not-yet-implemented 'fast--/signed=>signed x y r)))

(define-vop (fast--/unsigned=>unsigned)
  (:args (x :scs (unsigned-reg)) (y :scs (unsigned-reg)))
  (:results (r :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate -)
  (:policy :fast-safe)
  (:note "inline (unsigned-byte 32) arithmetic")
  (:generator 6 (vop-not-yet-implemented 'fast--/unsigned=>unsigned x y r)))

(define-vop (fast---c/fixnum=>fixnum)
  (:args (x :scs (any-reg)))
  (:results (r :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:constant short-immediate-fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:info y)
  (:translate -)
  (:policy :fast-safe)
  (:note "inline arithmetic")
  (:generator 1 (vop-not-yet-implemented 'fast---c/fixnum=>fixnum x r y)))

(define-vop (fast---c/signed=>signed)
  (:args (x :scs (signed-reg)))
  (:results (r :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:constant short-immediate))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:info y)
  (:translate -)
  (:policy :fast-safe)
  (:note "inline signed unboxed arithmetic")
  (:generator 5 (vop-not-yet-implemented 'fast---c/signed=>signed x r y)))

(define-vop (fast---c/unsigned=>unsigned)
  (:args (x :scs (unsigned-reg)))
  (:results (r :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:constant short-immediate))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:info y)
  (:translate -)
  (:policy :fast-safe)
  (:note "inline unsigned unboxed arithmetic")
  (:generator 5 (vop-not-yet-implemented 'fast---c/unsigned=>unsigned x r y)))

(define-vop (fast-logior/fixnum=>fixnum)
  (:args (x :scs (any-reg)) (y :scs (any-reg)))
  (:results (r :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:or positive-fixnum fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:translate logior)
  (:policy :fast-safe)
  (:note "inline fixnum arithmetic")
  (:generator 2 (vop-not-yet-implemented 'fast-logior/fixnum=>fixnum x y r)))

(define-vop (fast-logior/signed=>signed)
  (:args (x :scs (signed-reg)) (y :scs (signed-reg)))
  (:results (r :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate logior)
  (:policy :fast-safe)
  (:note "inline (signed-byte 32) arithmetic")
  (:generator 4 (vop-not-yet-implemented 'fast-logior/signed=>signed x y r)))

(define-vop (fast-logior/unsigned=>unsigned)
  (:args (x :scs (unsigned-reg)) (y :scs (unsigned-reg)))
  (:results (r :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate logior)
  (:policy :fast-safe)
  (:note "inline (unsigned-byte 32) arithmetic")
  (:generator 4 (vop-not-yet-implemented 'fast-logior/unsigned=>unsigned x y r)))

(define-vop (fast-logior-c/fixnum=>fixnum)
  (:args (x :scs (any-reg)))
  (:results (r :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:constant short-immediate-fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:info y)
  (:translate logior)
  (:policy :fast-safe)
  (:note "inline arithmetic")
  (:generator 1 (vop-not-yet-implemented 'fast-logior-c/fixnum=>fixnum x r y)))

(define-vop (fast-logior-c/signed=>signed)
  (:args (x :scs (signed-reg)))
  (:results (r :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:constant short-immediate))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:info y)
  (:translate logior)
  (:policy :fast-safe)
  (:note "inline signed unboxed arithmetic")
  (:generator 3 (vop-not-yet-implemented 'fast-logior-c/signed=>signed x r y)))

(define-vop (fast-logior-c/unsigned=>unsigned)
  (:args (x :scs (unsigned-reg)))
  (:results (r :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:constant short-immediate))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:info y)
  (:translate logior)
  (:policy :fast-safe)
  (:note "inline unsigned unboxed arithmetic")
  (:generator 3 (vop-not-yet-implemented 'fast-logior-c/unsigned=>unsigned x r y)))

(define-vop (fast-logxor/fixnum=>fixnum)
  (:args (x :scs (any-reg)) (y :scs (any-reg)))
  (:results (r :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:or positive-fixnum fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:translate logxor)
  (:policy :fast-safe)
  (:note "inline fixnum arithmetic")
  (:generator 2 (vop-not-yet-implemented 'fast-logxor/fixnum=>fixnum x y r)))

(define-vop (fast-logxor/signed=>signed)
  (:args (x :scs (signed-reg)) (y :scs (signed-reg)))
  (:results (r :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate logxor)
  (:policy :fast-safe)
  (:note "inline (signed-byte 32) arithmetic")
  (:generator 4 (vop-not-yet-implemented 'fast-logxor/signed=>signed x y r)))

(define-vop (fast-logxor/unsigned=>unsigned)
  (:args (x :scs (unsigned-reg)) (y :scs (unsigned-reg)))
  (:results (r :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate logxor)
  (:policy :fast-safe)
  (:note "inline (unsigned-byte 32) arithmetic")
  (:generator 4 (vop-not-yet-implemented 'fast-logxor/unsigned=>unsigned x y r)))

(define-vop (fast-logxor-c/fixnum=>fixnum)
  (:args (x :scs (any-reg)))
  (:results (r :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:constant short-immediate-fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:info y)
  (:translate logxor)
  (:policy :fast-safe)
  (:note "inline arithmetic")
  (:generator 1 (vop-not-yet-implemented 'fast-logxor-c/fixnum=>fixnum x r y)))

(define-vop (fast-logxor-c/signed=>signed)
  (:args (x :scs (signed-reg)))
  (:results (r :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:constant short-immediate))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:info y)
  (:translate logxor)
  (:policy :fast-safe)
  (:note "inline signed unboxed arithmetic")
  (:generator 3 (vop-not-yet-implemented 'fast-logxor-c/signed=>signed x r y)))

(define-vop (fast-logxor-c/unsigned=>unsigned)
  (:args (x :scs (unsigned-reg)))
  (:results (r :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:constant short-immediate))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:info y)
  (:translate logxor)
  (:policy :fast-safe)
  (:note "inline unsigned unboxed arithmetic")
  (:generator 3 (vop-not-yet-implemented 'fast-logxor-c/unsigned=>unsigned x r y)))

(define-vop (fast-logand/fixnum=>fixnum)
  (:args (x :scs (any-reg)) (y :scs (any-reg)))
  (:results (r :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:or positive-fixnum fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:translate logand)
  (:policy :fast-safe)
  (:note "inline fixnum arithmetic")
  (:generator 2 (vop-not-yet-implemented 'fast-logand/fixnum=>fixnum x y r)))

(define-vop (fast-logand/signed=>signed)
  (:args (x :scs (signed-reg)) (y :scs (signed-reg)))
  (:results (r :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate logand)
  (:policy :fast-safe)
  (:note "inline (signed-byte 32) arithmetic")
  (:generator 4 (vop-not-yet-implemented 'fast-logand/signed=>signed x y r)))

(define-vop (fast-logand/unsigned=>unsigned)
  (:args (x :scs (unsigned-reg)) (y :scs (unsigned-reg)))
  (:results (r :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate logand)
  (:policy :fast-safe)
  (:note "inline (unsigned-byte 32) arithmetic")
  (:generator 4 (vop-not-yet-implemented 'fast-logand/unsigned=>unsigned x y r)))

(define-vop (fast-logand-c/fixnum=>fixnum)
  (:args (x :scs (any-reg)))
  (:results (r :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:constant short-immediate-fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:info y)
  (:translate logand)
  (:policy :fast-safe)
  (:note "inline arithmetic")
  (:generator 1 (vop-not-yet-implemented 'fast-logand-c/fixnum=>fixnum x r y)))

(define-vop (fast-logand-c/signed=>signed)
  (:args (x :scs (signed-reg)))
  (:results (r :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:constant short-immediate))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:info y)
  (:translate logand)
  (:policy :fast-safe)
  (:note "inline signed unboxed arithmetic")
  (:generator 3 (vop-not-yet-implemented 'fast-logand-c/signed=>signed x r y)))

(define-vop (fast-logand-c/unsigned=>unsigned)
  (:args (x :scs (unsigned-reg)))
  (:results (r :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:constant short-immediate))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:info y)
  (:translate logand)
  (:policy :fast-safe)
  (:note "inline unsigned unboxed arithmetic")
  (:generator 3 (vop-not-yet-implemented 'fast-logand-c/unsigned=>unsigned x r y)))

(define-vop (fast-logand/signed-unsigned=>unsigned)
  (:args (x :scs (signed-reg)) (y :scs (unsigned-reg)))
  (:results (r :scs (unsigned-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate logand)
  (:policy :fast-safe)
  (:note "inline (unsigned-byte 32) arithmetic")
  (:generator 4 (vop-not-yet-implemented 'fast-logand/signed-unsigned=>unsigned x y r)))

(define-vop (fast-logand-c/signed-unsigned=>unsigned)
  (:args (x :scs (signed-reg)))
  (:results (r :scs (unsigned-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:constant (eql 4294967295)))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:info y)
  (:translate logand)
  (:policy :fast-safe)
  (:note "inline unsigned unboxed arithmetic")
  (:generator 1 (vop-not-yet-implemented 'fast-logand-c/signed-unsigned=>unsigned x r y)))

(define-vop (fast-ash-left-c/fixnum=>fixnum)
  (:args (number :scs (any-reg)))
  (:results (result :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:constant unsigned-byte))
  (:result-types (:or positive-fixnum fixnum))
  (:info amount)
  (:translate ash)
  (:policy :fast-safe)
  (:note "inline ASH")
  (:generator 1 (vop-not-yet-implemented 'fast-ash-left-c/fixnum=>fixnum number result amount)))

(define-vop (fast-ash-right-c/fixnum=>fixnum)
  (:args (number :scs (any-reg)))
  (:results (result :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:constant (integer * -1)))
  (:result-types (:or positive-fixnum fixnum))
  (:info amount)
  (:translate ash)
  (:policy :fast-safe)
  (:note "inline ASH")
  (:generator 1 (vop-not-yet-implemented 'fast-ash-right-c/fixnum=>fixnum number result amount)))

(define-vop (fast-ash-c/unsigned=>unsigned)
  (:args (number :scs (unsigned-reg)))
  (:results (result :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum) (:constant integer))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:info amount)
  (:translate ash)
  (:policy :fast-safe)
  (:note "inline ASH")
  (:generator 1 (vop-not-yet-implemented 'fast-ash-c/unsigned=>unsigned number result amount)))

(define-vop (fast-ash-c/signed=>signed)
  (:args (number :scs (signed-reg)))
  (:results (result :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:constant integer))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:info amount)
  (:translate ash)
  (:policy :fast-safe)
  (:note "inline ASH")
  (:generator 1 (vop-not-yet-implemented 'fast-ash-c/signed=>signed number result amount)))

(define-vop (fast-ash/signed/unsigned)
  (:args (number) (amount))
  (:results (result))
  (:policy :fast-safe)
  (:variant-vars variant)
  (:variant nil)
  (:note "inline ASH")
  (:generator 3 (vop-not-yet-implemented 'fast-ash/signed/unsigned number amount result variant)))

(define-vop (fast-ash/unsigned=>unsigned)
  (:args (number :scs (unsigned-reg)) (amount :scs (signed-reg)))
  (:results (result :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate ash)
  (:policy :fast-safe)
  (:variant-vars variant)
  (:variant :unsigned)
  (:note "inline ASH")
  (:generator 3 (vop-not-yet-implemented 'fast-ash/unsigned=>unsigned number amount result variant)))

(define-vop (fast-ash/signed=>signed)
  (:args (number :scs (signed-reg)) (amount :scs (signed-reg)))
  (:results (result :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate ash)
  (:policy :fast-safe)
  (:variant-vars variant)
  (:variant :signed)
  (:note "inline ASH")
  (:generator 3 (vop-not-yet-implemented 'fast-ash/signed=>signed number amount result variant)))

(define-vop (fast-ash-left/fixnum=>fixnum)
  (:args (number :scs (any-reg)) (amount :scs (signed-reg unsigned-reg)))
  (:results (result :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:or positive-fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:translate ash)
  (:policy :fast-safe)
  (:note "inline ASH")
  (:generator 2 (vop-not-yet-implemented 'fast-ash-left/fixnum=>fixnum number amount result)))

(define-vop (fast-ash-left/signed=>signed)
  (:args (number :scs (signed-reg)) (amount :scs (signed-reg unsigned-reg)))
  (:results (result :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:or positive-fixnum))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate ash)
  (:policy :fast-safe)
  (:note "inline ASH")
  (:generator 3 (vop-not-yet-implemented 'fast-ash-left/signed=>signed number amount result)))

(define-vop (fast-ash-left/unsigned=>unsigned)
  (:args (number :scs (unsigned-reg)) (amount :scs (signed-reg unsigned-reg)))
  (:results (result :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate ash)
  (:policy :fast-safe)
  (:note "inline ASH")
  (:generator 3 (vop-not-yet-implemented 'fast-ash-left/unsigned=>unsigned number amount result)))

(define-vop (fast-%ash/right/unsigned)
  (:args (number :scs (unsigned-reg)) (amount :scs (unsigned-reg)))
  (:results (result :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate %ash/right)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'fast-%ash/right/unsigned number amount result)))

(define-vop (fast-%ash/right/signed)
  (:args (number :scs (signed-reg)) (amount :scs (unsigned-reg)))
  (:results (result :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate %ash/right)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'fast-%ash/right/signed number amount result)))

(define-vop (fast-%ash/right/fixnum)
  (:args (number :scs (any-reg)) (amount :scs (unsigned-reg)))
  (:results (result :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:translate %ash/right)
  (:policy :fast-safe)
  (:generator 3 (vop-not-yet-implemented 'fast-%ash/right/fixnum number amount result)))

(define-vop (signed-byte-32-len)
  (:args (arg :scs (signed-reg)))
  (:results (res :scs (any-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or positive-fixnum))
  (:translate integer-length)
  (:policy :fast-safe)
  (:note "inline (signed-byte 32) integer-length")
  (:generator 30 (vop-not-yet-implemented 'signed-byte-32-len arg res)))

(define-vop (unsigned-byte-32-count)
  (:args (arg :scs (unsigned-reg)))
  (:results (res :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or positive-fixnum))
  (:translate logcount)
  (:policy :fast-safe)
  (:note "inline (unsigned-byte 32) logcount")
  (:generator 30 (vop-not-yet-implemented 'unsigned-byte-32-count arg res)))

(define-vop (fast-*/fixnum=>fixnum)
  (:args (x :scs (signed-reg)) (y :scs (any-reg)))
  (:results (r :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:or positive-fixnum fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:translate *)
  (:policy :fast-safe)
  (:note "inline fixnum arithmetic")
  (:generator 2 (vop-not-yet-implemented 'fast-*/fixnum=>fixnum x y r)))

(define-vop (fast-*/signed=>signed)
  (:args (x :scs (signed-reg)) (y :scs (signed-reg)))
  (:results (r :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate *)
  (:policy :fast-safe)
  (:note "inline (signed-byte 32) arithmetic")
  (:generator 3 (vop-not-yet-implemented 'fast-*/signed=>signed x y r)))

(define-vop (fast-*/unsigned=>unsigned)
  (:args (x :scs (unsigned-reg)) (y :scs (unsigned-reg)))
  (:results (r :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate *)
  (:policy :fast-safe)
  (:note "inline (unsigned-byte 32) arithmetic")
  (:generator 3 (vop-not-yet-implemented 'fast-*/unsigned=>unsigned x y r)))

(define-vop (fast-truncate/fixnum)
  (:args (x :scs (any-reg)) (y :scs (any-reg)))
  (:results (q :scs (any-reg)) (r :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:or positive-fixnum fixnum))
  (:result-types (:or positive-fixnum fixnum) (:or positive-fixnum fixnum))
  (:translate truncate)
  (:policy :fast-safe)
  (:save-p :compute-only)
  (:note "inline fixnum arithmetic")
  (:vop-var vop)
  (:generator 11 (vop-not-yet-implemented 'fast-truncate/fixnum x y q r)))

(define-vop (fast-truncate/unsigned)
  (:args (x :scs (unsigned-reg)) (y :scs (unsigned-reg)))
  (:results (q :scs (unsigned-reg)) (r :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate truncate)
  (:policy :fast-safe)
  (:save-p :compute-only)
  (:note "inline (unsigned-byte 32) arithmetic")
  (:vop-var vop)
  (:generator 12 (vop-not-yet-implemented 'fast-truncate/unsigned x y q r)))

(define-vop (fast-truncate/signed)
  (:args (x :scs (signed-reg)) (y :scs (signed-reg)))
  (:results (q :scs (signed-reg)) (r :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate truncate)
  (:policy :fast-safe)
  (:save-p :compute-only)
  (:note "inline (signed-byte 32) arithmetic")
  (:vop-var vop)
  (:generator 12 (vop-not-yet-implemented 'fast-truncate/signed x y q r)))

(define-vop (fast-conditional)
  (:args)
  (:result-types)
  (:info target not-p)
  (:conditional)
  (:policy :fast-safe)
  (:variant-vars condition)
  (:variant nil)
  (:generator 0 (vop-not-yet-implemented 'fast-conditional target not-p condition)))

(define-vop (fast-conditional/fixnum)
  (:args (x :scs (any-reg)) (y :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:or positive-fixnum fixnum))
  (:result-types)
  (:info target not-p)
  (:conditional)
  (:policy :fast-safe)
  (:variant-vars condition)
  (:variant nil)
  (:note "inline fixnum comparison")
  (:generator 1 (vop-not-yet-implemented 'fast-conditional/fixnum x y target not-p condition)))

(define-vop (fast-conditional/signed)
  (:args (x :scs (signed-reg)) (y :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types)
  (:info target not-p)
  (:conditional)
  (:policy :fast-safe)
  (:variant-vars condition)
  (:variant nil)
  (:note "inline (signed-byte 32) comparison")
  (:generator 1 (vop-not-yet-implemented 'fast-conditional/signed x y target not-p condition)))

(define-vop (fast-conditional/unsigned)
  (:args (x :scs (unsigned-reg)) (y :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types)
  (:info target not-p)
  (:conditional)
  (:policy :fast-safe)
  (:variant-vars condition)
  (:variant nil)
  (:note "inline (unsigned-byte 32) comparison")
  (:generator 1 (vop-not-yet-implemented 'fast-conditional/unsigned x y target not-p condition)))

(define-vop (fast-if-</fixnum)
  (:args (x :scs (any-reg)) (y :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:or positive-fixnum fixnum))
  (:result-types)
  (:info target not-p)
  (:conditional)
  (:translate <)
  (:policy :fast-safe)
  (:variant-vars condition)
  (:variant :lt)
  (:note "inline fixnum comparison")
  (:generator 1 (vop-not-yet-implemented 'fast-if-</fixnum x y target not-p condition)))

(define-vop (fast-if-</signed)
  (:args (x :scs (signed-reg)) (y :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types)
  (:info target not-p)
  (:conditional)
  (:translate <)
  (:policy :fast-safe)
  (:variant-vars condition)
  (:variant :lt)
  (:note "inline (signed-byte 32) comparison")
  (:generator 1 (vop-not-yet-implemented 'fast-if-</signed x y target not-p condition)))

(define-vop (fast-if-</unsigned)
  (:args (x :scs (unsigned-reg)) (y :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types)
  (:info target not-p)
  (:conditional)
  (:translate <)
  (:policy :fast-safe)
  (:variant-vars condition)
  (:variant :lt)
  (:note "inline (unsigned-byte 32) comparison")
  (:generator 1 (vop-not-yet-implemented 'fast-if-</unsigned x y target not-p condition)))

(define-vop (fast-if->/fixnum)
  (:args (x :scs (any-reg)) (y :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:or positive-fixnum fixnum))
  (:result-types)
  (:info target not-p)
  (:conditional)
  (:translate >)
  (:policy :fast-safe)
  (:variant-vars condition)
  (:variant :gt)
  (:note "inline fixnum comparison")
  (:generator 1 (vop-not-yet-implemented 'fast-if->/fixnum x y target not-p condition)))

(define-vop (fast-if->/signed)
  (:args (x :scs (signed-reg)) (y :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types)
  (:info target not-p)
  (:conditional)
  (:translate >)
  (:policy :fast-safe)
  (:variant-vars condition)
  (:variant :gt)
  (:note "inline (signed-byte 32) comparison")
  (:generator 1 (vop-not-yet-implemented 'fast-if->/signed x y target not-p condition)))

(define-vop (fast-if->/unsigned)
  (:args (x :scs (unsigned-reg)) (y :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types)
  (:info target not-p)
  (:conditional)
  (:translate >)
  (:policy :fast-safe)
  (:variant-vars condition)
  (:variant :gt)
  (:note "inline (unsigned-byte 32) comparison")
  (:generator 1 (vop-not-yet-implemented 'fast-if->/unsigned x y target not-p condition)))

(define-vop (fast-if-eql/signed)
  (:args (x :scs (signed-reg)) (y :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types)
  (:info target not-p)
  (:conditional)
  (:translate eql)
  (:policy :fast-safe)
  (:variant-vars condition)
  (:variant :eq)
  (:note "inline (signed-byte 32) comparison")
  (:generator 1 (vop-not-yet-implemented 'fast-if-eql/signed x y target not-p condition)))

(define-vop (fast-if-eql/unsigned)
  (:args (x :scs (unsigned-reg)) (y :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types)
  (:info target not-p)
  (:conditional)
  (:translate eql)
  (:policy :fast-safe)
  (:variant-vars condition)
  (:variant :eq)
  (:note "inline (unsigned-byte 32) comparison")
  (:generator 1 (vop-not-yet-implemented 'fast-if-eql/unsigned x y target not-p condition)))

(define-vop (fast-eql/fixnum)
  (:args (x :scs (any-reg)) (y :scs (any-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:or positive-fixnum fixnum))
  (:result-types)
  (:info target not-p)
  (:conditional)
  (:translate eql)
  (:policy :fast-safe)
  (:variant-vars condition)
  (:variant nil)
  (:note "inline fixnum comparison")
  (:generator 1 (vop-not-yet-implemented 'fast-eql/fixnum x y target not-p condition)))

(define-vop (generic-eql/fixnum)
  (:args (x :scs (any-reg descriptor-reg)) (y :scs (any-reg)))
  (:arg-types * (:or positive-fixnum fixnum))
  (:result-types)
  (:info target not-p)
  (:conditional)
  (:translate eql)
  (:policy :fast-safe)
  (:variant-vars condition)
  (:variant nil)
  (:note "inline fixnum comparison")
  (:generator 7 (vop-not-yet-implemented 'generic-eql/fixnum x y target not-p condition)))

(define-vop (shift-towards-start)
  (:args (num :scs (unsigned-reg)) (amount :scs (signed-reg)))
  (:results (r :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or positive-fixnum fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate shift-towards-start)
  (:policy :fast-safe)
  (:note "SHIFT-TOWARDS-START")
  (:generator 1 (vop-not-yet-implemented 'shift-towards-start num amount r)))

(define-vop (shift-towards-end)
  (:args (num :scs (unsigned-reg)) (amount :scs (signed-reg)))
  (:results (r :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or positive-fixnum fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate shift-towards-end)
  (:policy :fast-safe)
  (:note "SHIFT-TOWARDS-END")
  (:generator 1 (vop-not-yet-implemented 'shift-towards-end num amount r)))















(define-vop (fast-ash-left-mod32/unsigned=>unsigned)
  (:args (number :scs (unsigned-reg)) (amount :scs (signed-reg unsigned-reg)))
  (:results (result :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate ash)
  (:policy :fast-safe)
  (:note "inline ASH")
  (:generator 3 (vop-not-yet-implemented 'fast-ash-left-mod32/unsigned=>unsigned number amount result)))


(define-vop (bignum-length)
  (:args (x :scs (descriptor-reg)))
  (:results (res :scs (unsigned-reg)))
  (:result-types (:or positive-fixnum))
  (:translate sb-bignum:%bignum-length)
  (:policy :fast-safe)
  (:generator 6 (vop-not-yet-implemented 'bignum-length x res)))

(define-vop (bignum-set-length)
  (:args (x :scs (descriptor-reg)) (data :scs (any-reg immediate)))
  (:arg-types * (:or positive-fixnum))
  (:translate sb-bignum:%bignum-set-length)
  (:policy :fast-safe)
  (:generator 6 (vop-not-yet-implemented 'bignum-set-length x data)))

(define-vop (bignum-ref)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (unsigned-reg)))
  (:arg-types * (:or positive-fixnum fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate sb-bignum:%bignum-ref)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'bignum-ref object index value)))

(define-vop (bignum-ref-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (unsigned-reg)))
  (:arg-types * (:constant (load/store-index 4 7 1)))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:info index)
  (:translate sb-bignum:%bignum-ref)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'bignum-ref-c object value index)))

(define-vop (bignum-set)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (value :scs (unsigned-reg)))
  (:arg-types * (:or positive-fixnum fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate sb-bignum:%bignum-set)
  (:policy :fast-safe)
  (:generator 3 (vop-not-yet-implemented 'bignum-set object index value)))

(define-vop (bignum-set-c)
  (:args (object :scs (descriptor-reg)) (value :scs (unsigned-reg)))
  (:arg-types * (:constant (load/store-index 4 7 1))
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:info index)
  (:translate sb-bignum:%bignum-set)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented 'bignum-set-c object value index)))

(define-vop (digit-0-or-plus)
  (:args (digit :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types)
  (:info target not-p)
  (:conditional)
  (:translate sb-bignum:%digit-0-or-plusp)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented 'digit-0-or-plus digit target not-p)))

(define-vop (add-w/carry)
  (:args (a :scs (unsigned-reg)) (b :scs (unsigned-reg)) (c :scs (any-reg)))
  (:results (result :scs (unsigned-reg)) (carry :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum) (:or positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or positive-fixnum))
  (:translate sb-bignum:%add-with-carry)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'add-w/carry a b c result carry)))

(define-vop (sub-w/borrow)
  (:args (a :scs (unsigned-reg)) (b :scs (unsigned-reg)) (c :scs (any-reg)))
  (:results (result :scs (unsigned-reg)) (borrow :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum) (:or positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or positive-fixnum))
  (:translate sb-bignum:%subtract-with-borrow)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'sub-w/borrow a b c result borrow)))

(define-vop (bignum-mult-and-add-3-arg)
  (:args (x :scs (unsigned-reg)) (y :scs (unsigned-reg)) (carry-in :scs (unsigned-reg)))
  (:results (hi :scs (unsigned-reg)) (lo :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate sb-bignum:%multiply-and-add)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'bignum-mult-and-add-3-arg x y carry-in hi lo)))

(define-vop (bignum-mult-and-add-4-arg)
  (:args (x :scs (unsigned-reg)) (y :scs (unsigned-reg))
   (prev :scs (unsigned-reg)) (carry-in :scs (unsigned-reg)))
  (:results (hi :scs (unsigned-reg)) (lo :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate sb-bignum:%multiply-and-add)
  (:policy :fast-safe)
  (:generator 8 (vop-not-yet-implemented 'bignum-mult-and-add-4-arg x y prev carry-in hi lo)))

(define-vop (bignum-mult)
  (:args (x :scs (unsigned-reg)) (y :scs (unsigned-reg)))
  (:results (hi :scs (unsigned-reg)) (lo :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate sb-bignum:%multiply)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented 'bignum-mult x y hi lo)))

(define-vop (mulhi)
  (:args (x :scs (unsigned-reg)) (y :scs (unsigned-reg)))
  (:results (hi :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate %multiply-high)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented 'mulhi x y hi)))

(define-vop (mulhi/fx)
  (:args (x :scs (any-reg)) (y :scs (unsigned-reg)))
  (:results (hi :scs (any-reg)))
  (:arg-types (:or positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or positive-fixnum))
  (:translate %multiply-high)
  (:policy :fast-safe)
  (:generator 15 (vop-not-yet-implemented 'mulhi/fx x y hi)))

(define-vop (bignum-floor)
  (:args (num-high :scs (unsigned-reg))
   (num-low :scs (unsigned-reg)) (denom :scs (unsigned-reg)))
  (:results (quo :scs (unsigned-reg)) (rem :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate sb-bignum:%bigfloor)
  (:policy :fast-safe)
  (:generator 325 (vop-not-yet-implemented 'bignum-floor num-high num-low denom quo rem)))

(define-vop (signify-digit)
  (:args (digit :scs (unsigned-reg)))
  (:results (res :scs (any-reg signed-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate sb-bignum:%fixnum-digit-with-correct-sign)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented 'signify-digit digit res)))

(define-vop (digit-ashr)
  (:args (digit :scs (unsigned-reg)) (count :scs (unsigned-reg)))
  (:results (result :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate sb-bignum:%ashr)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented 'digit-ashr digit count result)))

(define-vop (digit-lshr)
  (:args (digit :scs (unsigned-reg)) (count :scs (unsigned-reg)))
  (:results (result :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate sb-bignum:%digit-logical-shift-right)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented 'digit-lshr digit count result)))

(define-vop (digit-ashl)
  (:args (digit :scs (unsigned-reg)) (count :scs (unsigned-reg)))
  (:results (result :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate sb-bignum:%ashl)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented 'digit-ashl digit count result)))

(define-vop (signed-byte-32-p)
  (:args (value :scs (any-reg descriptor-reg)))
  (:result-types)
  (:info target not-p)
  (:conditional)
  (:translate signed-byte-32-p)
  (:policy :fast-safe)
  (:generator 10 (vop-not-yet-implemented 'signed-byte-32-p value target not-p)))

(define-vop (unsigned-byte-32-p)
  (:args (value :scs (any-reg descriptor-reg)))
  (:result-types)
  (:info target not-p)
  (:conditional)
  (:translate unsigned-byte-32-p)
  (:policy :fast-safe)
  (:generator 10 (vop-not-yet-implemented 'unsigned-byte-32-p value target not-p)))
