;;;; sap VOPs for the WebAssembly target.
;;;;
;;;; Sprint 2 placeholder: the VOPs are written in the next sprint (see
;;;; doc/wasm-port/04-sprints.md). The file exists so that the backend
;;;; builds.

;;;; This software is part of the SBCL system. See the README file for
;;;; more information.

(in-package "SB-VM")

;;;; Skeletons: operand shapes taken from the riscv backend, generators to
;;;; be written in Sprint 3. Generated by Sprints/Sprint2/gen-skeletons.lisp.

(define-vop (move-to-sap)
  (:args (x :scs (any-reg descriptor-reg)))
  (:results (y :scs (sap-reg)))
  (:note "pointer to SAP coercion")
  (:generator 1 (vop-not-yet-implemented 'move-to-sap x y)))

(define-vop (move-from-sap)
  (:args (sap :scs (sap-reg)))
  (:results (res :scs (descriptor-reg)))
  (:note "SAP to pointer coercion")
  (:generator 20 (vop-not-yet-implemented 'move-from-sap sap res)))

(define-vop (sap-move)
  (:args (x :scs (sap-reg) :load-if (not (location= x y))))
  (:results (y :scs (sap-reg) :load-if (not (location= x y))))
  (:note "SAP move")
  
  (:generator 0 (vop-not-yet-implemented 'sap-move x y)))

(define-vop (move-sap-arg)
  (:args (x :scs (sap-reg))
   (fp :scs (any-reg) :load-if (not (sc-is y sap-reg))))
  (:results (y))
  (:note "SAP argument move")
  (:generator 0 (vop-not-yet-implemented 'move-sap-arg x fp y)))

(define-vop (sap-int)
  (:args (sap :scs (sap-reg)))
  (:results (int :scs (unsigned-reg)))
  (:arg-types (:or system-area-pointer))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate sap-int)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented 'sap-int sap int)))

(define-vop (int-sap)
  (:args (int :scs (unsigned-reg)))
  (:results (sap :scs (sap-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or system-area-pointer))
  (:translate int-sap)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented 'int-sap int sap)))

(define-vop (pointer+)
  (:args (ptr :scs (sap-reg)) (offset :scs (signed-reg)))
  (:results (res :scs (sap-reg)))
  (:arg-types (:or system-area-pointer)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or system-area-pointer))
  (:translate sap+)
  (:policy :fast-safe)
  (:generator 2 (vop-not-yet-implemented 'pointer+ ptr offset res)))

(define-vop (pointer+-unsigned-c)
  (:args (ptr :scs (sap-reg)))
  (:results (res :scs (sap-reg)))
  (:arg-types (:or system-area-pointer) (:constant short-immediate))
  (:result-types (:or system-area-pointer))
  (:info offset)
  (:translate sap+)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented 'pointer+-unsigned-c ptr res offset)))

(define-vop (pointer-)
  (:args (ptr1 :scs (sap-reg)) (ptr2 :scs (sap-reg)))
  (:results (res :scs (signed-reg)))
  (:arg-types (:or system-area-pointer) (:or system-area-pointer))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate sap-)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented 'pointer- ptr1 ptr2 res)))

(define-vop (sap-ref-8)
  (:args (object :scs (sap-reg)) (offset :scs (signed-reg)))
  (:results (result :scs (unsigned-reg)))
  (:arg-types (:or system-area-pointer)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or positive-fixnum))
  (:translate sap-ref-8)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'sap-ref-8 object offset result)))

(define-vop (sap-ref-8-c)
  (:args (sap :scs (sap-reg)))
  (:results (result :scs (unsigned-reg)))
  (:arg-types (:or system-area-pointer) (:constant short-immediate))
  (:result-types (:or positive-fixnum))
  (:info offset)
  (:translate sap-ref-8)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'sap-ref-8-c sap result offset)))

(define-vop (%set-sap-ref-8)
  (:args (value :scs (unsigned-reg)) (object :scs (sap-reg))
   (offset :scs (signed-reg)))
  (:arg-types (:or positive-fixnum) (:or system-area-pointer)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate %set-sap-ref-8)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented '%set-sap-ref-8 value object offset)))

(define-vop (%set-sap-ref-8-c)
  (:args (value :scs (unsigned-reg)) (sap :scs (sap-reg)))
  (:arg-types (:or positive-fixnum) (:or system-area-pointer) (:constant short-immediate))
  (:info offset)
  (:translate %set-sap-ref-8)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented '%set-sap-ref-8-c value sap offset)))

(define-vop (signed-sap-ref-8)
  (:args (object :scs (sap-reg)) (offset :scs (signed-reg)))
  (:results (result :scs (signed-reg)))
  (:arg-types (:or system-area-pointer)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:translate signed-sap-ref-8)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'signed-sap-ref-8 object offset result)))

(define-vop (signed-sap-ref-8-c)
  (:args (sap :scs (sap-reg)))
  (:results (result :scs (signed-reg)))
  (:arg-types (:or system-area-pointer) (:constant short-immediate))
  (:result-types (:or positive-fixnum fixnum))
  (:info offset)
  (:translate signed-sap-ref-8)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'signed-sap-ref-8-c sap result offset)))

(define-vop (%set-signed-sap-ref-8)
  (:args (value :scs (signed-reg)) (object :scs (sap-reg))
   (offset :scs (signed-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:or system-area-pointer)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate %set-signed-sap-ref-8)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented '%set-signed-sap-ref-8 value object offset)))

(define-vop (%set-signed-sap-ref-8-c)
  (:args (value :scs (signed-reg)) (sap :scs (sap-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:or system-area-pointer)
   (:constant short-immediate))
  (:info offset)
  (:translate %set-signed-sap-ref-8)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented '%set-signed-sap-ref-8-c value sap offset)))

(define-vop (sap-ref-16)
  (:args (object :scs (sap-reg)) (offset :scs (signed-reg)))
  (:results (result :scs (unsigned-reg)))
  (:arg-types (:or system-area-pointer)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or positive-fixnum))
  (:translate sap-ref-16)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'sap-ref-16 object offset result)))

(define-vop (sap-ref-16-c)
  (:args (sap :scs (sap-reg)))
  (:results (result :scs (unsigned-reg)))
  (:arg-types (:or system-area-pointer) (:constant short-immediate))
  (:result-types (:or positive-fixnum))
  (:info offset)
  (:translate sap-ref-16)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'sap-ref-16-c sap result offset)))

(define-vop (%set-sap-ref-16)
  (:args (value :scs (unsigned-reg)) (object :scs (sap-reg))
   (offset :scs (signed-reg)))
  (:arg-types (:or positive-fixnum) (:or system-area-pointer)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate %set-sap-ref-16)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented '%set-sap-ref-16 value object offset)))

(define-vop (%set-sap-ref-16-c)
  (:args (value :scs (unsigned-reg)) (sap :scs (sap-reg)))
  (:arg-types (:or positive-fixnum) (:or system-area-pointer) (:constant short-immediate))
  (:info offset)
  (:translate %set-sap-ref-16)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented '%set-sap-ref-16-c value sap offset)))

(define-vop (signed-sap-ref-16)
  (:args (object :scs (sap-reg)) (offset :scs (signed-reg)))
  (:results (result :scs (signed-reg)))
  (:arg-types (:or system-area-pointer)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or positive-fixnum fixnum))
  (:translate signed-sap-ref-16)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'signed-sap-ref-16 object offset result)))

(define-vop (signed-sap-ref-16-c)
  (:args (sap :scs (sap-reg)))
  (:results (result :scs (signed-reg)))
  (:arg-types (:or system-area-pointer) (:constant short-immediate))
  (:result-types (:or positive-fixnum fixnum))
  (:info offset)
  (:translate signed-sap-ref-16)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'signed-sap-ref-16-c sap result offset)))

(define-vop (%set-signed-sap-ref-16)
  (:args (value :scs (signed-reg)) (object :scs (sap-reg))
   (offset :scs (signed-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:or system-area-pointer)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate %set-signed-sap-ref-16)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented '%set-signed-sap-ref-16 value object offset)))

(define-vop (%set-signed-sap-ref-16-c)
  (:args (value :scs (signed-reg)) (sap :scs (sap-reg)))
  (:arg-types (:or positive-fixnum fixnum) (:or system-area-pointer)
   (:constant short-immediate))
  (:info offset)
  (:translate %set-signed-sap-ref-16)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented '%set-signed-sap-ref-16-c value sap offset)))

(define-vop (cas-sap-ref-32)
  (:args (oldval :scs (unsigned-reg)) (newval :scs (unsigned-reg)) (sap :scs (sap-reg))
   (offset :scs (signed-reg)))
  (:results (result :scs (unsigned-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum) (:or system-area-pointer)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate (cas sap-ref-32))
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'cas-sap-ref-32 oldval newval sap offset result)))

(define-vop (sap-ref-32)
  (:args (object :scs (sap-reg)) (offset :scs (signed-reg)))
  (:results (result :scs (unsigned-reg)))
  (:arg-types (:or system-area-pointer)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate sap-ref-32)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'sap-ref-32 object offset result)))

(define-vop (sap-ref-32-c)
  (:args (sap :scs (sap-reg)))
  (:results (result :scs (unsigned-reg)))
  (:arg-types (:or system-area-pointer) (:constant short-immediate))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:info offset)
  (:translate sap-ref-32)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'sap-ref-32-c sap result offset)))

(define-vop (%set-sap-ref-32)
  (:args (value :scs (unsigned-reg)) (object :scs (sap-reg))
   (offset :scs (signed-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or system-area-pointer) (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate %set-sap-ref-32)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented '%set-sap-ref-32 value object offset)))

(define-vop (%set-sap-ref-32-c)
  (:args (value :scs (unsigned-reg)) (sap :scs (sap-reg)))
  (:arg-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or system-area-pointer) (:constant short-immediate))
  (:info offset)
  (:translate %set-sap-ref-32)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented '%set-sap-ref-32-c value sap offset)))

(define-vop (signed-sap-ref-32)
  (:args (object :scs (sap-reg)) (offset :scs (signed-reg)))
  (:results (result :scs (signed-reg)))
  (:arg-types (:or system-area-pointer)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate signed-sap-ref-32)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'signed-sap-ref-32 object offset result)))

(define-vop (signed-sap-ref-32-c)
  (:args (sap :scs (sap-reg)))
  (:results (result :scs (signed-reg)))
  (:arg-types (:or system-area-pointer) (:constant short-immediate))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:info offset)
  (:translate signed-sap-ref-32)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'signed-sap-ref-32-c sap result offset)))

(define-vop (%set-signed-sap-ref-32)
  (:args (value :scs (signed-reg)) (object :scs (sap-reg))
   (offset :scs (signed-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:or system-area-pointer) (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate %set-signed-sap-ref-32)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented '%set-signed-sap-ref-32 value object offset)))

(define-vop (%set-signed-sap-ref-32-c)
  (:args (value :scs (signed-reg)) (sap :scs (sap-reg)))
  (:arg-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:or system-area-pointer) (:constant short-immediate))
  (:info offset)
  (:translate %set-signed-sap-ref-32)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented '%set-signed-sap-ref-32-c value sap offset)))

(define-vop (cas-sap-ref-sap)
  (:args (oldval :scs (sap-reg)) (newval :scs (sap-reg)) (sap :scs (sap-reg))
   (offset :scs (signed-reg)))
  (:results (result :scs (sap-reg)))
  (:arg-types (:or system-area-pointer) (:or system-area-pointer)
   (:or system-area-pointer) (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or system-area-pointer))
  (:translate (cas sap-ref-sap))
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'cas-sap-ref-sap oldval newval sap offset result)))

(define-vop (sap-ref-sap)
  (:args (object :scs (sap-reg)) (offset :scs (signed-reg)))
  (:results (result :scs (sap-reg)))
  (:arg-types (:or system-area-pointer)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or system-area-pointer))
  (:translate sap-ref-sap)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'sap-ref-sap object offset result)))

(define-vop (sap-ref-sap-c)
  (:args (sap :scs (sap-reg)))
  (:results (result :scs (sap-reg)))
  (:arg-types (:or system-area-pointer) (:constant short-immediate))
  (:result-types (:or system-area-pointer))
  (:info offset)
  (:translate sap-ref-sap)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'sap-ref-sap-c sap result offset)))

(define-vop (%set-sap-ref-sap)
  (:args (value :scs (sap-reg)) (object :scs (sap-reg))
   (offset :scs (signed-reg)))
  (:arg-types (:or system-area-pointer) (:or system-area-pointer)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate %set-sap-ref-sap)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented '%set-sap-ref-sap value object offset)))

(define-vop (%set-sap-ref-sap-c)
  (:args (value :scs (sap-reg)) (sap :scs (sap-reg)))
  (:arg-types (:or system-area-pointer) (:or system-area-pointer)
   (:constant short-immediate))
  (:info offset)
  (:translate %set-sap-ref-sap)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented '%set-sap-ref-sap-c value sap offset)))

(define-vop (cas-sap-ref-lispobj)
  (:args (oldval :scs (descriptor-reg)) (newval :scs (descriptor-reg))
   (sap :scs (sap-reg)) (offset :scs (signed-reg)))
  (:results (result :scs (descriptor-reg)))
  (:arg-types * * (:or system-area-pointer)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types *)
  (:translate (cas sap-ref-lispobj))
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'cas-sap-ref-lispobj oldval newval sap offset result)))

(define-vop (sap-ref-lispobj)
  (:args (object :scs (sap-reg)) (offset :scs (signed-reg)))
  (:results (result :scs (descriptor-reg)))
  (:arg-types (:or system-area-pointer)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types *)
  (:translate sap-ref-lispobj)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'sap-ref-lispobj object offset result)))

(define-vop (sap-ref-lispobj-c)
  (:args (sap :scs (sap-reg)))
  (:results (result :scs (descriptor-reg)))
  (:arg-types (:or system-area-pointer) (:constant short-immediate))
  (:result-types *)
  (:info offset)
  (:translate sap-ref-lispobj)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'sap-ref-lispobj-c sap result offset)))

(define-vop (%set-sap-ref-lispobj)
  (:args (value :scs (descriptor-reg)) (object :scs (sap-reg))
   (offset :scs (signed-reg)))
  (:arg-types * (:or system-area-pointer)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate %set-sap-ref-lispobj)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented '%set-sap-ref-lispobj value object offset)))

(define-vop (%set-sap-ref-lispobj-c)
  (:args (value :scs (descriptor-reg)) (sap :scs (sap-reg)))
  (:arg-types * (:or system-area-pointer) (:constant short-immediate))
  (:info offset)
  (:translate %set-sap-ref-lispobj)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented '%set-sap-ref-lispobj-c value sap offset)))

(define-vop (sap-ref-single)
  (:args (object :scs (sap-reg)) (offset :scs (signed-reg)))
  (:results (result :scs (single-reg)))
  (:arg-types (:or system-area-pointer)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or single-float))
  (:translate sap-ref-single)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'sap-ref-single object offset result)))

(define-vop (sap-ref-single-c)
  (:args (sap :scs (sap-reg)))
  (:results (result :scs (single-reg)))
  (:arg-types (:or system-area-pointer) (:constant short-immediate))
  (:result-types (:or single-float))
  (:info offset)
  (:translate sap-ref-single)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'sap-ref-single-c sap result offset)))

(define-vop (%set-sap-ref-single)
  (:args (value :scs (single-reg)) (object :scs (sap-reg))
   (offset :scs (signed-reg)))
  (:arg-types (:or single-float) (:or system-area-pointer)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate %set-sap-ref-single)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented '%set-sap-ref-single value object offset)))

(define-vop (%set-sap-ref-single-c)
  (:args (value :scs (single-reg)) (sap :scs (sap-reg)))
  (:arg-types (:or single-float) (:or system-area-pointer) (:constant short-immediate))
  (:info offset)
  (:translate %set-sap-ref-single)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented '%set-sap-ref-single-c value sap offset)))

(define-vop (sap-ref-double)
  (:args (object :scs (sap-reg)) (offset :scs (signed-reg)))
  (:results (result :scs (double-reg)))
  (:arg-types (:or system-area-pointer)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or double-float))
  (:translate sap-ref-double)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'sap-ref-double object offset result)))

(define-vop (sap-ref-double-c)
  (:args (sap :scs (sap-reg)))
  (:results (result :scs (double-reg)))
  (:arg-types (:or system-area-pointer) (:constant short-immediate))
  (:result-types (:or double-float))
  (:info offset)
  (:translate sap-ref-double)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'sap-ref-double-c sap result offset)))

(define-vop (%set-sap-ref-double)
  (:args (value :scs (double-reg)) (object :scs (sap-reg))
   (offset :scs (signed-reg)))
  (:arg-types (:or double-float) (:or system-area-pointer)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate %set-sap-ref-double)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented '%set-sap-ref-double value object offset)))

(define-vop (%set-sap-ref-double-c)
  (:args (value :scs (double-reg)) (sap :scs (sap-reg)))
  (:arg-types (:or double-float) (:or system-area-pointer) (:constant short-immediate))
  (:info offset)
  (:translate %set-sap-ref-double)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented '%set-sap-ref-double-c value sap offset)))

(define-vop (vector-sap)
  (:args (vector :scs (descriptor-reg)))
  (:results (sap :scs (sap-reg)))
  (:result-types (:or system-area-pointer))
  (:translate vector-sap)
  (:policy :fast-safe)
  (:generator 2 (vop-not-yet-implemented 'vector-sap vector sap)))
