;;;; cell VOPs for the WebAssembly target.
;;;;
;;;; Sprint 2 placeholder: the VOPs are written in the next sprint (see
;;;; doc/wasm-port/04-sprints.md). The file exists so that the backend
;;;; builds.

;;;; This software is part of the SBCL system. See the README file for
;;;; more information.

(in-package "SB-VM")

;;;; Skeletons: operand shapes taken from the riscv backend, generators to
;;;; be written in Sprint 3. Generated by Sprints/Sprint2/gen-skeletons.lisp.

(define-vop (%set-symbol-global-value)
  (:args (object :scs (descriptor-reg)) (value :scs (descriptor-reg any-reg)))
  (:variant-vars offset lowtag)
  (:variant symbol-value-slot other-pointer-lowtag)
  (:generator 4 (vop-not-yet-implemented '%set-symbol-global-value object value offset lowtag)))

(define-vop (closure-init)
  (:args (object :scs (descriptor-reg)) (value :scs (descriptor-reg any-reg)))
  (:info offset dx)
  (:generator 4 (vop-not-yet-implemented 'closure-init object value offset dx)))

(define-vop (closure-init-from-fp)
  (:args (object :scs (descriptor-reg)))
  (:info offset)
  (:generator 4 (vop-not-yet-implemented 'closure-init-from-fp object offset)))

(define-vop (closure-ref)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (descriptor-reg any-reg)))
  (:info offset)
  (:generator 4 (vop-not-yet-implemented 'closure-ref object value offset)))

(define-vop (compare-and-swap-slot)
  (:args (object :scs (descriptor-reg)) (old :scs (descriptor-reg any-reg))
   (new :scs (descriptor-reg any-reg)))
  (:results (result :scs (descriptor-reg)))
  (:info name offset lowtag)
  (:generator 5 (vop-not-yet-implemented 'compare-and-swap-slot object old new result name offset lowtag)))

(define-vop (dynbind)
  (:args (value :scs (any-reg descriptor-reg)) (symbol :scs (descriptor-reg)))
  (:generator 5 (vop-not-yet-implemented 'dynbind value symbol)))


(define-vop (fast-symbol-global-value)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (descriptor-reg any-reg)))
  (:translate symbol-global-value)
  (:variant-vars offset lowtag)
  (:variant symbol-value-slot other-pointer-lowtag)
  (:generator 4 (vop-not-yet-implemented 'fast-symbol-global-value object value offset lowtag)))

(define-vop (fast-symbol-value)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (descriptor-reg any-reg)))
  (:translate symbol-value)
  (:variant-vars offset lowtag)
  (:variant symbol-value-slot other-pointer-lowtag)
  (:generator 4 (vop-not-yet-implemented 'fast-symbol-value object value offset lowtag)))


(define-vop (safe-fdefn-fun)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (descriptor-reg any-reg)))
  (:translate safe-fdefn-fun)
  (:policy :fast-safe)
  (:save-p :compute-only)
  (:vop-var vop)
  (:generator 10 (vop-not-yet-implemented 'safe-fdefn-fun object value)))

(define-vop (set)
  (:args (object :scs (descriptor-reg)) (value :scs (descriptor-reg any-reg)))
  (:variant-vars offset lowtag)
  (:variant symbol-value-slot other-pointer-lowtag)
  (:generator 4 (vop-not-yet-implemented 'set object value offset lowtag)))

(define-vop (set-slot)
  (:args (object :scs (descriptor-reg)) (value :scs (descriptor-reg any-reg)))
  (:info name offset lowtag)
  (:generator 1 (vop-not-yet-implemented 'set-slot object value name offset lowtag)))

(define-vop (slot)
  (:args (object :scs (descriptor-reg)))
  (:results (result :scs (descriptor-reg any-reg)))
  (:info name offset lowtag)
  (:generator 1 (vop-not-yet-implemented 'slot object result name offset lowtag)))

(define-vop (symbol-global-value)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (descriptor-reg any-reg)))
  (:translate symbol-global-value)
  (:policy :fast-safe)
  (:save-p :compute-only)
  (:vop-var vop)
  (:generator 9 (vop-not-yet-implemented 'symbol-global-value object value)))

(define-vop (symbol-value)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (descriptor-reg any-reg)))
  (:translate symbol-value)
  (:policy :fast-safe)
  (:save-p :compute-only)
  (:vop-var vop)
  (:generator 9 (vop-not-yet-implemented 'symbol-value object value)))


(define-vop (unbind)
  (:args)
  (:generator 0 (vop-not-yet-implemented 'unbind )))

(define-vop (unbind-to-here)
  (:args (arg :scs (descriptor-reg any-reg)))
  (:generator 0 (vop-not-yet-implemented 'unbind-to-here arg)))



(define-vop (value-cell-set)
  (:args (object :scs (descriptor-reg)) (value :scs (descriptor-reg any-reg)))
  (:variant-vars offset lowtag)
  (:variant value-cell-value-slot other-pointer-lowtag)
  (:generator 4 (vop-not-yet-implemented 'value-cell-set object value offset lowtag)))

;;;; Skeletons: operand shapes taken from the riscv backend, generators to
;;;; be written in Sprint 3. Generated by Sprints/Sprint2/gen-skeletons.lisp.


;;;; Skeletons: operand shapes taken from the riscv backend, generators to
;;;; be written in Sprint 3. Generated by Sprints/Sprint2/gen-skeletons.lisp.

(define-vop (%raw-instance-ref/word)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (unsigned-reg)))
  (:arg-types * (:or positive-fixnum fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate %raw-instance-ref/word)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented '%raw-instance-ref/word object index value)))

(define-vop (%raw-instance-ref/signed-word)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (signed-reg)))
  (:arg-types * (:or positive-fixnum fixnum))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate %raw-instance-ref/signed-word)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented '%raw-instance-ref/signed-word object index value)))

(define-vop (%raw-instance-ref/single)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (single-reg)))
  (:arg-types * (:or positive-fixnum fixnum))
  (:result-types (:or single-float))
  (:translate %raw-instance-ref/single)
  (:policy :fast-safe)
  (:note "raw instance access")
  (:generator 5 (vop-not-yet-implemented '%raw-instance-ref/single object index value)))

(define-vop (%raw-instance-ref/double)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (double-reg)))
  (:arg-types * (:or positive-fixnum fixnum))
  (:result-types (:or double-float))
  (:translate %raw-instance-ref/double)
  (:policy :fast-safe)
  (:note "raw instance access")
  (:generator 5 (vop-not-yet-implemented '%raw-instance-ref/double object index value)))

(define-vop (%raw-instance-ref/complex-single)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (complex-single-reg)))
  (:arg-types * (:or positive-fixnum fixnum))
  (:result-types (:or complex-single-float))
  (:translate %raw-instance-ref/complex-single)
  (:policy :fast-safe)
  (:note "raw instance access")
  (:generator 6 (vop-not-yet-implemented '%raw-instance-ref/complex-single object index value)))

(define-vop (%raw-instance-ref/complex-double)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (complex-double-reg)))
  (:arg-types * (:or positive-fixnum fixnum))
  (:result-types (:or complex-double-float))
  (:translate %raw-instance-ref/complex-double)
  (:policy :fast-safe)
  (:note "raw instance access")
  (:generator 6 (vop-not-yet-implemented '%raw-instance-ref/complex-double object index value)))

(define-vop (%raw-instance-set/word)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (value :scs (unsigned-reg)))
  (:arg-types * (:or positive-fixnum fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate %raw-instance-set/word)
  (:policy :fast-safe)
  (:generator 3 (vop-not-yet-implemented '%raw-instance-set/word object index value)))

(define-vop (%raw-instance-set/signed-word)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)) (value :scs (signed-reg)))
  (:arg-types * (:or positive-fixnum fixnum)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate %raw-instance-set/signed-word)
  (:policy :fast-safe)
  (:generator 3 (vop-not-yet-implemented '%raw-instance-set/signed-word object index value)))

(define-vop (%raw-instance-set/single)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)) (value :scs (single-reg)))
  (:arg-types * (:or positive-fixnum fixnum) (:or single-float))
  (:translate %raw-instance-set/single)
  (:policy :fast-safe)
  (:note "raw instance store")
  (:generator 5 (vop-not-yet-implemented '%raw-instance-set/single object index value)))

(define-vop (%raw-instance-set/double)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)) (value :scs (double-reg)))
  (:arg-types * (:or positive-fixnum fixnum) (:or double-float))
  (:translate %raw-instance-set/double)
  (:policy :fast-safe)
  (:note "raw instance store")
  (:generator 5 (vop-not-yet-implemented '%raw-instance-set/double object index value)))

(define-vop (%raw-instance-set/complex-single)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (value :scs (complex-single-reg)))
  (:arg-types * (:or positive-fixnum fixnum) (:or complex-single-float))
  (:translate %raw-instance-set/complex-single)
  (:policy :fast-safe)
  (:note "raw instance store")
  (:generator 6 (vop-not-yet-implemented '%raw-instance-set/complex-single object index value)))

(define-vop (%raw-instance-set/complex-double)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (value :scs (complex-double-reg)))
  (:arg-types * (:or positive-fixnum fixnum) (:or complex-double-float))
  (:translate %raw-instance-set/complex-double)
  (:policy :fast-safe)
  (:note "raw instance store")
  (:generator 6 (vop-not-yet-implemented '%raw-instance-set/complex-double object index value)))

;;;; Skeletons: operand shapes taken from the riscv backend, generators to
;;;; be written in Sprint 3. Generated by Sprints/Sprint2/gen-skeletons.lisp.


(define-vop (%compare-and-swap-symbol-value)
  (:args (symbol :scs (descriptor-reg)) (old :scs (descriptor-reg any-reg))
   (new :scs (descriptor-reg any-reg)))
  (:results (result :scs (descriptor-reg any-reg)))
  (:translate %compare-and-swap-symbol-value)
  (:policy :fast-safe)
  (:vop-var vop)
  (:generator 15 (vop-not-yet-implemented '%compare-and-swap-symbol-value symbol old new result)))

(define-vop (checked-cell-ref)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (descriptor-reg any-reg)))
  (:policy :fast-safe)
  (:save-p :compute-only)
  (:vop-var vop)
  (:generator 0 (vop-not-yet-implemented 'checked-cell-ref object value)))

(define-vop (boundp)
  (:args (object :scs (descriptor-reg)))
  (:result-types)
  (:info target not-p)
  (:conditional)
  (:translate boundp)
  (:policy :fast-safe)
  (:generator 9 (vop-not-yet-implemented 'boundp object target not-p)))

(define-vop (set-fdefn-fun)
  (:args (function :scs (descriptor-reg)) (fdefn :scs (descriptor-reg)))
  (:policy :fast-safe)
  (:generator 3 (vop-not-yet-implemented 'set-fdefn-fun function fdefn)))

(define-vop (fdefn-makunbound)
  (:args (fdefn :scs (descriptor-reg)))
  (:translate fdefn-makunbound)
  (:policy :fast-safe)
  (:generator 38 (vop-not-yet-implemented 'fdefn-makunbound fdefn)))

(define-vop (closure-index-ref)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (descriptor-reg any-reg)))
  (:arg-types * (:or positive-fixnum fixnum))
  (:result-types *)
  (:translate %closure-index-ref)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'closure-index-ref object index value)))

(define-vop (closure-index-ref-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (descriptor-reg any-reg)))
  (:arg-types * (:constant (load/store-index 4 5 2)))
  (:result-types *)
  (:info index)
  (:translate %closure-index-ref)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'closure-index-ref-c object value index)))

(define-vop (%closure-index-set)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (value :scs (descriptor-reg any-reg)))
  (:arg-types * (:or positive-fixnum fixnum) *)
  (:translate %closure-index-set)
  (:policy :fast-safe)
  (:generator 3 (vop-not-yet-implemented '%closure-index-set object index value)))

(define-vop (%closure-index-set-c)
  (:args (object :scs (descriptor-reg)) (value :scs (descriptor-reg any-reg)))
  (:arg-types * (:constant (load/store-index 4 5 2)) *)
  (:info index)
  (:translate %closure-index-set)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented '%closure-index-set-c object value index)))

(define-vop (funcallable-instance-info)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (descriptor-reg any-reg)))
  (:arg-types * (:or positive-fixnum fixnum))
  (:result-types *)
  (:translate %funcallable-instance-info)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'funcallable-instance-info object index value)))

(define-vop (funcallable-instance-info-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (descriptor-reg any-reg)))
  (:arg-types * (:constant (load/store-index 4 5 4)))
  (:result-types *)
  (:info index)
  (:translate %funcallable-instance-info)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'funcallable-instance-info-c object value index)))

(define-vop (%instance-length)
  (:args (struct :scs (descriptor-reg)))
  (:results (res :scs (unsigned-reg)))
  (:result-types (:or positive-fixnum))
  (:translate %instance-length)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented '%instance-length struct res)))

(define-vop (instance-index-ref)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg)))
  (:results (value :scs (descriptor-reg any-reg)))
  (:arg-types * (:or positive-fixnum fixnum))
  (:result-types *)
  (:translate %instance-ref)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'instance-index-ref object index value)))

(define-vop (instance-index-ref-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (descriptor-reg any-reg)))
  (:arg-types * (:constant (load/store-index 4 1 1)))
  (:result-types *)
  (:info index)
  (:translate %instance-ref)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented 'instance-index-ref-c object value index)))

(define-vop (instance-index-set)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (value :scs (descriptor-reg any-reg)))
  (:arg-types * (:or positive-fixnum fixnum) *)
  (:translate %instance-set)
  (:policy :fast-safe)
  (:generator 3 (vop-not-yet-implemented 'instance-index-set object index value)))

(define-vop (instance-index-set-c)
  (:args (object :scs (descriptor-reg)) (value :scs (descriptor-reg any-reg)))
  (:arg-types * (:constant (load/store-index 4 1 1)) *)
  (:info index)
  (:translate %instance-set)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented 'instance-index-set-c object value index)))

(define-vop (instance-index-cas)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (old-value :scs (descriptor-reg any-reg)) (new-value :scs (descriptor-reg any-reg)))
  (:results (result :scs (descriptor-reg any-reg)))
  (:arg-types * (:or positive-fixnum) * *)
  (:result-types *)
  (:translate %instance-cas)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'instance-index-cas object index old-value new-value result)))

(define-vop (code-header-set)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (value :scs (any-reg descriptor-reg)))
  (:arg-types * (:or positive-fixnum fixnum) *)
  (:translate code-header-set)
  (:policy :fast-safe)
  (:generator 10 (vop-not-yet-implemented 'code-header-set object index value)))

(define-vop (%raw-instance-ref/word-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (unsigned-reg)))
  (:arg-types * (:constant (load/store-index 4 1 1)))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:info index)
  (:translate %raw-instance-ref/word)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented '%raw-instance-ref/word-c object value index)))

(define-vop (%raw-instance-set/word-c)
  (:args (object :scs (descriptor-reg)) (value :scs (unsigned-reg)))
  (:arg-types * (:constant (load/store-index 4 1 1))
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:info index)
  (:translate %raw-instance-set/word)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented '%raw-instance-set/word-c object value index)))

(define-vop (raw-instance-cas/word)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (old-value :scs (unsigned-reg)) (new-value :scs (unsigned-reg)))
  (:results (result :scs (unsigned-reg)))
  (:arg-types (:or instance) (:or positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate %raw-instance-cas/word)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'raw-instance-cas/word object index old-value new-value result)))

(define-vop (%raw-instance-ref/signed-word-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (signed-reg)))
  (:arg-types * (:constant (load/store-index 4 1 1)))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:info index)
  (:translate %raw-instance-ref/signed-word)
  (:policy :fast-safe)
  (:generator 4 (vop-not-yet-implemented '%raw-instance-ref/signed-word-c object value index)))

(define-vop (%raw-instance-set/signed-word-c)
  (:args (object :scs (descriptor-reg)) (value :scs (signed-reg)))
  (:arg-types * (:constant (load/store-index 4 1 1))
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:info index)
  (:translate %raw-instance-set/signed-word)
  (:policy :fast-safe)
  (:generator 1 (vop-not-yet-implemented '%raw-instance-set/signed-word-c object value index)))

(define-vop (raw-instance-cas/signed-word)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (old-value :scs (signed-reg)) (new-value :scs (signed-reg)))
  (:results (result :scs (signed-reg)))
  (:arg-types (:or instance) (:or positive-fixnum)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum)
   (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:result-types (:or signed-byte-32 fixnum unsigned-byte-31 positive-fixnum))
  (:translate %raw-instance-cas/signed-word)
  (:policy :fast-safe)
  (:generator 5 (vop-not-yet-implemented 'raw-instance-cas/signed-word object index old-value new-value result)))

(define-vop (%raw-instance-ref/single-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (single-reg)))
  (:arg-types * (:constant (load/store-index 4 1 1)))
  (:result-types (:or single-float))
  (:info index)
  (:translate %raw-instance-ref/single)
  (:policy :fast-safe)
  (:note "raw instance access")
  (:generator 4 (vop-not-yet-implemented '%raw-instance-ref/single-c object value index)))

(define-vop (%raw-instance-set/single-c)
  (:args (object :scs (descriptor-reg)) (value :scs (single-reg)))
  (:arg-types * (:constant (load/store-index 4 1 1)) (:or single-float))
  (:info index)
  (:translate %raw-instance-set/single)
  (:policy :fast-safe)
  (:note "raw instance store")
  (:generator 4 (vop-not-yet-implemented '%raw-instance-set/single-c object value index)))

(define-vop (%raw-instance-ref/double-c)
  (:args (object :scs (descriptor-reg)))
  (:results (value :scs (double-reg)))
  (:arg-types * (:constant (load/store-index 4 1 1)))
  (:result-types (:or double-float))
  (:info index)
  (:translate %raw-instance-ref/double)
  (:policy :fast-safe)
  (:note "raw instance access")
  (:generator 4 (vop-not-yet-implemented '%raw-instance-ref/double-c object value index)))

(define-vop (%raw-instance-set/double-c)
  (:args (object :scs (descriptor-reg)) (value :scs (double-reg)))
  (:arg-types * (:constant (load/store-index 4 1 1)) (:or double-float))
  (:info index)
  (:translate %raw-instance-set/double)
  (:policy :fast-safe)
  (:note "raw instance store")
  (:generator 4 (vop-not-yet-implemented '%raw-instance-set/double-c object value index)))

(define-vop (raw-instance-incf/word)
  (:args (object :scs (descriptor-reg)) (index :scs (any-reg))
   (operand :scs (unsigned-reg)))
  (:results (result :scs (unsigned-reg)))
  (:arg-types * (:or positive-fixnum)
   (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:result-types (:or unsigned-byte-32 unsigned-byte-31 positive-fixnum))
  (:translate %raw-instance-atomic-incf/word)
  (:policy :fast-safe)
  (:generator 3 (vop-not-yet-implemented 'raw-instance-incf/word object index operand result)))
